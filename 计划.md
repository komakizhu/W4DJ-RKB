# W4DJ 独立歌曲库 Dashboard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `执行计划代理` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Do not dispatch subagents from a side conversation.

**Goal:** 建立 W4DJ 自己的本地 SQLite 分析歌曲库，只收录已经完成分析并写入分析缓存的歌曲，支持增量分析、可配置事实表格、结构化检索和分模式元数据回写。网易云数据库仅在转换阶段作为元数据来源，不再作为 Dashboard 歌曲库来源。

**Architecture:** 网易云数据库始终只读，并只在转换阶段读取用于元数据补全/写入；`library-dashboard.sqlite3` 仅作为兼容的元数据暂存与写回库。W4DJ 使用独立的 `w4dj.sqlite3` 登记已经安全提交的实际输出产物，Dashboard 只查询该独立库，不发现、打开或导入网易云数据库，也不直接在前端执行任意 SQL。

**Tech Stack:** Rust 2024、`rusqlite` bundled SQLite、Tauri 2、TypeScript、Vitest、现有 Essentia.js 分析链路、现有 MP3/FLAC/NCM/WAV/AIFF 元数据读取能力。

> **2026-08-25 文件名规则优先修正（已实现）：** 输出文件名优先遵循用户选择的 `FilenameRule`：`TitleArtist` 使用 `歌名 - 歌手`，`ArtistTitle` 使用 `歌手 - 歌名`，`Original` 才直接使用源文件 basename。源文件原名不再覆盖用户规则；源 basename 只在所需元数据缺失时兜底。文件名与元数据分离：元数据、封面和数据库匹配继续使用未清洗的源身份；文件名仅将无法作为 macOS 路径组件的 NUL 替换为 `, `，ASCII `/` 替换为全角 `／`，其它合法字符保持不变。任务 2（普通文件夹/SoundCloud 槽位）既有规则不变。五首真实样本已在隔离临时目录完成扫描、转换、目标文件名、源 Title/Artist/封面回读验收；真实用户输出目录和 W4DJ 数据库未被改写。

> **5 首 NUL 歌手真实验收清单：** `Kalawanji - Kromestar,Cessman.mp3`、`Waiting For Tremor (COOL BROS Edit).mp3`、`バギー・ブギー／Buggy Boogie.mp3`、`爱是甜的.mp3`、`通撒美.mp3`。当前规则为 `TitleArtist` 时，5 首必须生成“歌名 - 歌手”目标名；NUL 只在文件名中显示为 `, `，源 Artist 标签、Title、封面和 W4DJ 数据库匹配不得改变。验收还必须确认没有 `unexpected NUL byte`，10 首标题含 ASCII `/` 的歌曲不会创建意外子目录。

> **2026-08-25 验收记录：** 五首样本均按 `TitleArtist` 生成目标名，未出现 `unexpected NUL byte`；实际 FFmpeg 转换写入隔离临时目录，输出 Title/Artist 和封面帧与源标签一致。五首样本的用户目录、现有输出、W4DJ SQLite 和兼容分析缓存未被修改。10 首 ASCII `/` 标题的批量真实转换、ExifTool 全字段回读和 W4DJ SQLite 绑定仍属于下一轮现场验收。

## Global Constraints

- 网易云原数据库只允许以 SQLite read-only 模式打开，不写入、不迁移、不建索引。
- Dashboard 只收录已完成分析的歌曲；未分析歌曲和仅存在于网易云数据库的记录不显示。
- 任务 1 不在启动或教程结束时自动打开网易云数据库；输入区域仍允许点击选择、拖入单曲或文件夹、清空和重新选择。来源标题旁的“扫描本地网易云文件夹”先调用现有自动发现并把 `musicFolder` 填入任务 1，自动发现失败后才显示“手动选择文件夹”兜底，不参与 Dashboard 歌曲枚举。
- 任务 2 继续作为自定义来源，输入与输出都允许修改，保留单曲和文件夹选择能力。
- 任务 1 和任务 2 的输入区域都增加与输出目录同款的“打开文件夹”按钮；只有当前输入是文件夹或可定位到父目录的单曲时可用。
- 首次使用教程完成后不启动网易云数据库发现，避免数据库扫描遮挡教程；网易云数据库只在转换阶段按既有匹配链路读取元数据。
- macOS 与 Windows 使用同一领域接口，仅数据库候选路径和文件打开方式按平台实现。
- 文件大小、格式和平均码率以本地文件实测为准；没有可读本地文件时才回退到网易云数据库值。
- 时长优先级为：启用且成功的 Essentia 结果 > 本地媒体探测结果 > 网易云数据库值，并保存 `duration_source` 供界面解释。
- 网易云 Genre、W4DJ Genre（Discogs-EffNet）与 W4DJ Style（MusiCNN 50 标签）分列保存、分列展示，任何一方都不得覆盖另一方。
- 普通转换使用与 NCM 解密一致的核心标签集：歌曲名、完整歌手列表、专辑和封面。当数据库匹配通过既有高置信度标准时，这四项以数据库为权威值回写；无可靠匹配时保留输入文件元数据，不猜测关联。
- 增强转换在核心标签之上，回写数据库中可验证的 Genre、别名、版权、发布日期和歌词，并合并 Essentia 分析结果；分析结果不得覆盖歌曲名、歌手、专辑和封面等身份数据。
- 曲目 ID 和专辑 ID 仅用于内部匹配、去重和封面查找，不写入导出音频、Rekordbox XML 可见字段或转换报告的用户可见区域。
- 格式、码率、文件大小和时长属于产物实测技术属性：完整保存到 Dashboard 与报告，但不伪造为音频标签；输出文件中的这些值始终以转换后实测为准。
- `volumeDelta` 不得当作 LUFS；网易云质量字段与 Essentia Loudness 分开保存。
- Dashboard 的“刷新”只重新查询 W4DJ 输出 SQLite，不触发格式转换、不写转换历史；新的分析结果由增强转换链路完成后绑定输出路径增量写入。
- 现有 `scan-cache.json` 和 `track-analysis.json` 在本阶段继续作为转换扫描缓存和增强分析缓存；Dashboard SQLite 保存它们的查询投影，不在本计划中强制迁移或删除旧缓存。
- 不新增任意 SQL 输入框；筛选条件必须经过字段和操作符白名单转换为参数化 SQL。
- 不自动提交、推送或创建 Release；版本号仅在用户明确要求时更新。实现完成后展示测试、`git status`、diff 摘要和 Apple Silicon App 地址，等待用户确认。
- 当前工作树已有用户改动；实施时只修改本计划列出的文件，不清理未跟踪文件，不破坏已有改动。

> **2026-08-19 数据源边界修正：** Dashboard 不再读取网易云数据库；`library-dashboard.sqlite3` 只保留兼容元数据暂存/写回。网易云发现/数据库刷新命令保留为兼容接口，但不由 Dashboard 或教程后流程调用，网易云数据库只服务转换阶段元数据路径。下面只勾选能够由源码和测试直接证明的步骤。

> **2026-08-19 独立输出歌曲库重构：** 上述边界进一步收紧：`<app-data>/w4dj.sqlite3` 是 Dashboard 唯一权威数据源，歌曲记录以成功提交的实际输出文件为准，普通转换登记为 `notAnalyzed`，增强分析完成后绑定同一 `destination_path` 更新分析状态。`track-analysis.json` 只保留兼容镜像；网易云 SQLite 不得枚举 Dashboard 歌曲。输出目录偏好改变不会立即改库，任务槽第一次在新目录成功产出后才切换生效；失效扫描和清理只操作 W4DJ SQLite 记录。

> **2026-08-18 Essentia 模型交付扩展：** TensorFlow.js 与 MusiCNN、五个 Mood 头、人声/器乐头、emoMusic/MuSe/MIREX 及 Discogs EffNet/Genre 资源已随 App 离线内置（当前资源约 25 MB）；启动自动补齐缺失/损坏模型，不再依赖运行时下载。普通设置区已移除恢复内置模型、官方网页和手动导入按钮，模型拖入也不再触发导入；Rust 校验/恢复/导入命令作为兼容维护能力保留。内置资源已通过 Rust 安装校验、TensorFlow.js 真实离线加载/零输入推理、Vite 无 CDN 检查和 Apple App 资源检查；真实歌曲分类与 GUI 仍需人工验收。

> **2026-08-22 Genre/Style 双轨模型决策：** 产品层保留两个智能字段：`Genre` 使用 Discogs-EffNet 的细分主风格（例如 Deep House、Tech House、Shoegaze），`Style` 使用 MSD-MusiCNN 的 50 个多标签输出（例如 80s、Electronic、Dance、Happy、Female Vocalists）。当前由 MusiCNN 50 标签规则投影出的旧 Essentia Genre 不再作为新 Genre；旧记录迁移时只能归入 Style，不能伪装成 Discogs Genre。网易云 Genre 继续作为独立来源字段。Style 必须保留多个标签和置信度，Genre 界面可显示最高置信度主标签，同时保留候选标签供详情和查询使用。

> **2026-08-22 情绪模型验证阶段决策：** 先把 `emomusic-msd-musicnn`、`muse-msd-musicnn` 和 `moods_mirex-msd-musicnn` 三套能力接入后台数据结构、Worker 协议、离线测试夹具和可选模型注册，但暂不把它们放到 Dashboard 默认界面，也不急于把三套模型都作为强制内置资源。前两者各自输出 1–9 范围的 Valence/Arousal，必须分别保存、分别评测，不能互相覆盖或平均；后者输出五组 MIREX 情绪簇，单独保存，不能替代连续坐标。现有 aggressive/happy/relaxed/party/sad 五个二分类头继续作为 Mood Tags，五个头与这三套新能力可以并行产出，但不得混入同一数组。真实歌曲测试完成后，再选择要公开的字段和要随包内置的模型；模型缺失时保留基础分析，Genre/Style/Emotion/MIREX 分别报告缺失，不互相冒充。

---

## 产品行为定稿

### 主界面任务槽

任务 1 保留原有输入框，不在启动或教程完成后自动搜索网易云数据库。来源标题旁的“扫描本地网易云文件夹”按钮先调用 `locateNeteaseLibrary(true)`，将成功返回的 `musicFolder` 通过现有任务 1 来源流程保存；只有自动定位失败时才把按钮变为“手动选择文件夹”，输入区域仍可点击选择、拖入、清空和替换。

该按钮只使用只读网易云数据库定位本地音乐目录，不把网易云记录写入 Dashboard，也不会触发歌曲库刷新或增强分析；网易云数据库仍只在转换阶段提供元数据。

两个任务的输入区域都在垃圾桶左侧增加“打开文件夹”图标，位置、尺寸、hover 和禁用态与输出目录同款。来源是单曲时打开其所在文件夹并选中文件；来源是目录时直接打开目录；来源为空或不可读时按钮禁用。

Dashboard 只收录已经完成分析并写入 W4DJ SQLite 投影的歌曲。任务 1 选择网易云目录或其他来源时，转换仍按现有通用文件/文件夹流程处理；网易云数据库匹配只用于转换阶段的元数据读取和写回，不会把数据库-only 记录加入 Dashboard。

### Dashboard 入口与刷新

右上角增加带图标和文字的“歌曲库”按钮。打开 Dashboard 只读取 W4DJ SQLite 分析投影；如果为空，提示用户先在增强模式完成分析。用户点击“刷新”时只重新查询当前投影，不定位、打开或导入网易云数据库，也不触发新的分析或转换。

### 表格与详情

默认列为：封面、歌曲名、歌手、专辑、本地状态、格式、平均码率、文件大小、时长、网易云 Genre、Genre、Style、BPM、Key、Loudness、Energy、歌词状态、最后更新。别名、版权和发布日期作为可选列提供。表头可拖动重排，可调整宽度，可隐藏，设置保存在 W4DJ 前端偏好中。

点击歌曲行打开详情抽屉，分为：

```text
标准化信息
本地文件实测
网易云原始信息
Essentia 分析
原始数据库记录
```

“原始数据库记录”按来源表展示完整 JSON，不把几十个内部字段全部挤入主表。

### 检索规则

用户通过可视化条件编辑器构造条件，不直接输入 SQL。第一阶段支持：

```text
is / is not
contains / does not contain
starts with / ends with
> / >= / < / <=
between
is empty / is not empty
is true / is false
```

条件组支持 `AND`、`OR`，排序支持多列升序、降序。全文搜索覆盖歌曲名、歌手、专辑、文件名、网易云 Genre、Genre 和 Style。查询必须分页，默认每页 100 条。

### 转换模式与元数据回写标准

“普通转换”与“增强转换”共用同一个网易云歌曲匹配结果，但写入范围不同。匹配必须记录数据来源和置信情况；不能为了补标签而将低置信度数据库记录写给错误歌曲。

| 字段 | 普通转换 | 增强转换 | Dashboard / 报告 |
| --- | --- | --- | --- |
| 歌曲名 | 写入 | 写入 | 保存来源值和有效值 |
| 完整歌手列表 | 写入，不丢失多歌手 | 写入 | 保存原始列表和展示字符串 |
| 专辑 | 写入 | 写入 | 保存 |
| 封面 | 写入内嵌封面 | 写入内嵌封面 | 保存来源、MIME、像素和校验结果 |
| 网易云 Genre | 不扩展 NCM 核心标签集 | 写入标准 Genre | 与 W4DJ Genre、Style 分开保存 |
| W4DJ Genre | 不写入 | Discogs-EffNet 主风格（模型可用且达到阈值时） | 保存主标签、候选标签和置信度 |
| Style | 不写入 | MSD-MusiCNN 50 个多标签（模型可用时） | 保存多个标签、置信度和标签来源 |
| 别名、版权、发布日期 | 不写入 | 按容器能力写入标准标签，无标准字段时写入分析备注 | 保存原始值与写入去向 |
| 歌词 | 按下方歌词策略写标准标签 | 同普通转换 | 保存完整歌词、语言、同步类型和来源 |
| Essentia 结果 | 不运行、不写入 | 写入已有标准字段和现有分析备注；Genre/Style 分别按模型来源写入 | 保存完整结果、置信度和过滤原因 |
| 曲目 ID、专辑 ID | 不写入 | 不写入 | 仅在内部索引和原始记录中保存 |
| 格式、码率、文件大小、时长 | 不作为标签伪造 | 不作为标签伪造 | 转换后重新实测并保存 |

输出容器映射遵循：MP3 使用 ID3v2.4，FLAC 使用 Vorbis Comment 与 `METADATA_BLOCK_PICTURE`，AIFF 使用 ID3 chunk，WAV 优先 RIFF INFO。Rekordbox 对 WAV 可见标签的官方支持范围是 RIFF INFO，因此 WAV 不得只写入 Rekordbox 无法读取的私有 chunk。每种格式写入后必须重新读取并在报告中列出“计划写入 / 实际读回 / 未支持”。

Genre 的音频标签规则为：网易云 Genre、W4DJ Genre 和 Style 在 W4DJ 索引库永远分列。增强转换时，标准音频 Genre 仍优先写入网易云的非空值，只有其为空时才使用达到阈值的 Discogs W4DJ Genre；Style 只作为多标签分析结果写入分析备注、W4DJ SQLite 和兼容镜像，不伪装成标准 Genre。旧 Essentia Genre 投影只能迁移为 Style，不能在没有 Discogs 推理的情况下填充新 Genre。

### 歌词保存与查看策略

W4DJ 将歌词视为歌曲库资料，不依赖 Rekordbox 作为唯一查看器。索引库分开保存原文、翻译、罗马音/音译、是否带时间轴、原始 LRC 和来源表，Dashboard 详情抽屉增加“歌词”页签，支持搜索、复制、原文/翻译切换和按时间轴滚动（有时间轴时）。

导出时使用通用元数据标准：MP3/AIFF 写入 ID3 `USLT`，如实现库稳定支持同步歌词则额外写入 `SYLT`；FLAC 写入 `LYRICS` 并在有原始时间轴时同目录输出 `.lrc`；WAV 无稳定跨软件歌词标签时，只输出 `.lrc` 并在 Dashboard 保存。`.lrc` 文件与音频同名，写入临时文件后原子替换，不因歌词失败删除已成功的音频。

Rekordbox 不作为内嵌歌词的验收终点：官方的 Lyric 功能已于 2024 年 11 月关闭，且官方文档没有保证当前版本会从本地 `USLT`/`LYRICS` 标签展示歌词。Rekordbox XML 只同步歌曲名、歌手、专辑、Genre、Comments 等其实际支持字段，不把整段歌词塞入 Comments。W4DJ Dashboard 和同名 `.lrc` 是可靠查看方案。

兼容性依据：[rekordbox 官方 Lyric 功能关闭说明](https://rekordbox.com/en/support/faq/lyric-6/)；[rekordbox 6.7 官方手册的可读音频标签说明](https://cdn.rekordbox.com/files/20230316171900/rekordbox6.7.0_manual_EN.pdf)。

---

## 数据模型与优先级

### W4DJ 索引库位置

使用 Tauri `app_data_dir`：

```text
<app-data>/w4dj.sqlite3
```

不得将索引库写入网易云目录或音乐源目录。

`<app-data>/library-dashboard.sqlite3` 仍可由兼容网易云刷新命令使用，但仅承担旧元数据暂存/写回职责，不得作为 Dashboard、分析状态或候选歌曲来源。

### Schema version 1

```sql
CREATE TABLE catalog_meta (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

CREATE TABLE tracks (
    track_key TEXT PRIMARY KEY,
    netease_track_id TEXT,
    title TEXT NOT NULL DEFAULT '',
    artists TEXT NOT NULL DEFAULT '',
    artist_list_json TEXT NOT NULL DEFAULT '[]',
    album TEXT NOT NULL DEFAULT '',
    aliases_json TEXT NOT NULL DEFAULT '[]',
    copyright_text TEXT NOT NULL DEFAULT '',
    publish_date TEXT NOT NULL DEFAULT '',
    netease_genre TEXT NOT NULL DEFAULT '',
    essentia_genre TEXT NOT NULL DEFAULT '',
    lyric_plain_text TEXT NOT NULL DEFAULT '',
    lyric_translated_text TEXT NOT NULL DEFAULT '',
    lyric_romanized_text TEXT NOT NULL DEFAULT '',
    lyric_lrc_text TEXT NOT NULL DEFAULT '',
    lyric_language TEXT NOT NULL DEFAULT '',
    lyric_sync_type TEXT NOT NULL DEFAULT 'none',
    lyric_source TEXT NOT NULL DEFAULT '',
    cover_path TEXT,
    cover_available INTEGER NOT NULL DEFAULT 0,
    local_status TEXT NOT NULL,
    preferred_local_file_id INTEGER,
    db_duration_seconds REAL,
    measured_duration_seconds REAL,
    essentia_duration_seconds REAL,
    effective_duration_seconds REAL,
    duration_source TEXT,
    db_format TEXT,
    measured_format TEXT,
    effective_format TEXT,
    db_bitrate_bps INTEGER,
    measured_bitrate_bps INTEGER,
    effective_bitrate_bps INTEGER,
    db_size_bytes INTEGER,
    measured_size_bytes INTEGER,
    effective_size_bytes INTEGER,
    bpm REAL,
    musical_key TEXT,
    scale TEXT,
    integrated_loudness_lufs REAL,
    loudness_range_lu REAL,
    energy REAL,
    danceability REAL,
    mood_json TEXT NOT NULL DEFAULT '[]',
    instrument_json TEXT NOT NULL DEFAULT '[]',
    drop_loudness_lufs REAL,
    updated_at_ms INTEGER NOT NULL,
    FOREIGN KEY(preferred_local_file_id) REFERENCES local_files(id)
);

CREATE UNIQUE INDEX tracks_netease_id
ON tracks(netease_track_id)
WHERE netease_track_id IS NOT NULL AND netease_track_id <> '';

CREATE INDEX tracks_title ON tracks(title COLLATE NOCASE);
CREATE INDEX tracks_artists ON tracks(artists COLLATE NOCASE);
CREATE INDEX tracks_album ON tracks(album COLLATE NOCASE);
CREATE INDEX tracks_local_status ON tracks(local_status);
CREATE INDEX tracks_bpm ON tracks(bpm);

CREATE TABLE local_files (
    id INTEGER PRIMARY KEY,
    track_key TEXT NOT NULL,
    path TEXT NOT NULL UNIQUE,
    size_bytes INTEGER NOT NULL,
    modified_at_ms INTEGER,
    measured_format TEXT,
    measured_bitrate_bps INTEGER,
    measured_duration_seconds REAL,
    sample_rate_hz INTEGER,
    channels INTEGER,
    readable INTEGER NOT NULL,
    probe_error TEXT,
    FOREIGN KEY(track_key) REFERENCES tracks(track_key) ON DELETE CASCADE
);

CREATE INDEX local_files_track_key ON local_files(track_key);

CREATE TABLE source_records (
    id INTEGER PRIMARY KEY,
    track_key TEXT NOT NULL,
    source_table TEXT NOT NULL,
    source_primary_key TEXT NOT NULL,
    source_version TEXT,
    raw_json TEXT NOT NULL,
    imported_at_ms INTEGER NOT NULL,
    UNIQUE(source_table, source_primary_key),
    FOREIGN KEY(track_key) REFERENCES tracks(track_key) ON DELETE CASCADE
);

CREATE INDEX source_records_track_key ON source_records(track_key);

CREATE TABLE catalog_sources (
    database_path TEXT PRIMARY KEY,
    database_size_bytes INTEGER NOT NULL,
    database_modified_at_ms INTEGER,
    last_imported_at_ms INTEGER NOT NULL,
    import_status TEXT NOT NULL,
    last_error TEXT
);
```

`track_key` 生成规则：有网易云歌曲 ID 时使用 `netease:<track_id>`；没有歌曲 ID 时使用 `source:<table>:<primary_key>`。不得以歌曲名作为主键。

### 字段来源优先级

```rust
fn effective_duration(
    essentia: Option<f64>,
    measured: Option<f64>,
    database: Option<f64>,
) -> (Option<f64>, Option<DurationSource>) {
    essentia
        .filter(|value| value.is_finite() && *value > 0.0)
        .map(|value| (Some(value), Some(DurationSource::Essentia)))
        .or_else(|| measured.filter(|value| value.is_finite() && *value > 0.0)
            .map(|value| (Some(value), Some(DurationSource::Measured))))
        .or_else(|| database.filter(|value| value.is_finite() && *value > 0.0)
            .map(|value| (Some(value), Some(DurationSource::Netease))))
        .unwrap_or((None, None))
}
```

格式、文件大小和平均码率不使用 Essentia 值：

```rust
effective_format = measured_format.or(db_format);
effective_size_bytes = measured_size_bytes.or(db_size_bytes);
effective_bitrate_bps = measured_bitrate_bps.or(db_bitrate_bps);
```

Genre 永远分开：

```rust
struct CatalogGenres {
    netease_genre: String,
    genre: String,
    style_tags_json: String,
}
```

### 独立输出库附加表

`w4dj.sqlite3` 在共享 `tracks`/`local_files` 事实表之外增加 `library_meta`、`output_roots`、`slot_output_roots`、`w4dj_track_meta` 和 `analysis_results`。`w4dj_track_meta.destination_path` 是输出歌曲的唯一绑定，`status` 固定为 `available`、`outOfScope`、`missing` 或 `unreadable`，`analysis_status` 固定为 `notAnalyzed`、`failed` 或 `completed`。Dashboard 查询必须通过 `EXISTS w4dj_track_meta` 过滤，不能因为共享表中存在旧网易云记录而显示 database-only 歌曲。

---

## 文件结构

### 新建

- `src/library_catalog.rs`：SQLite schema、事务、增量 upsert、字段优先级和 catalog repository。
- `src/w4dj_library.rs`：输出拥有的 `w4dj.sqlite3` schema、历史导入、输出登记、A/B 根目录状态、分析绑定和失效扫描/清理。
- `src/library_query.rs`：字段白名单、筛选 AST、参数化 SQL、分页和排序。
- `src/media_probe.rs`：本地文件实测格式、大小、平均码率、时长、采样率和声道数。
- `src/netease_library.rs`：macOS/Windows 数据库候选定位、只读快照、原始记录归一化和本地文件解析。
- `src/lyrics.rs`：网易云歌词归一化、LRC 时间轴解析、标准音频标签与同名 `.lrc` 输出规则。
- `app/src/library-dashboard.ts`：Dashboard 状态、表格渲染、详情抽屉、筛选器和列配置。
- `app/src/library-dashboard.test.ts`：Dashboard 前端行为测试。
- `tests/library_catalog.rs`：索引库、导入、增量更新、优先级和查询集成测试。
- `tests/w4dj_library.rs`：输出登记、A/B 根目录切换、失效清理和未分析记录边界测试。

### 修改

- `src/lib.rs`：导出新模块。
- `src/netease.rs`：复用已有 JSON 解析和封面定位能力，不复制解析规则。
- `src/analysis.rs`：提供按路径读取现有分析缓存投影的纯函数。
- `src/metadata.rs`：增加普通/增强两套写入策略、分容器字段映射和写后读回校验。
- `src/desktop.rs`：保留转换阶段的网易云来源匹配能力，并保留两个任务槽的通用手动来源行为；不在启动或教程后自动发现数据库。
- `src/preferences.rs`：保存已选择的网易云数据库备用路径和 Dashboard 偏好版本。
- `src-tauri/src/main.rs`：注册 catalog 状态、后台刷新、查询、详情和手动定位命令。
- `app/src/app.ts`：增加歌曲库入口、任务槽角色渲染及 Dashboard 服务连接。
- `app/src/styles.css`：Dashboard、事实表格、详情抽屉、筛选器及任务 1 只读状态。
- `app/src/app.test.ts`：任务槽语义、入口和服务调用测试。
- `Cargo.toml`：若现有元数据能力不能稳定提供实测时长和码率，增加固定版本的纯 Rust 媒体探测依赖；实施者必须先证明现有能力不足再新增依赖。

---

### Task 1: 建立 Catalog 领域模型和 SQLite Repository

> **当前状态：部分完成。** schema、事务 CRUD、Genre 分列、时长优先级和 Catalog 测试已存在；原始 TDD 失败过程、完整外键覆盖和“只出现本任务文件”的隔离检查不能从当前工作树证明。

**Files:**
- Create: `src/library_catalog.rs`
- Modify: `src/lib.rs`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `LibraryCatalog::open(path)`, `LibraryCatalog::migrate()`, `LibraryCatalog::upsert_snapshot(snapshot)`, `LibraryCatalog::track_detail(track_key)`。
- Produces: `CatalogTrack`, `CatalogLocalFile`, `CatalogSourceRecord`, `DurationSource`, `LocalStatus`。

- [ ] **Step 1: 写失败测试，验证 schema、外键和 Genre 分列**

```rust
#[test]
fn catalog_keeps_netease_and_essentia_genres_separate() {
    let directory = tempfile::tempdir().unwrap();
    let catalog = LibraryCatalog::open(&directory.path().join("library.sqlite3")).unwrap();
    catalog.migrate().unwrap();
    catalog.upsert_track(CatalogTrackFixture::new("netease:28712318")
        .netease_genre("J-Pop")
        .essentia_genre("City Pop")
        .build()).unwrap();
    let track = catalog.track_detail("netease:28712318").unwrap().unwrap();
    assert_eq!(track.netease_genre, "J-Pop");
    assert_eq!(track.essentia_genre, "City Pop");
}
```

- [ ] **Step 2: 运行测试并确认因模块不存在而失败**

Run: `cargo test --test library_catalog catalog_keeps_netease_and_essentia_genres_separate`

Expected: FAIL，提示 `library_catalog` 或 `LibraryCatalog` 未定义。

- [x] **Step 3: 实现 schema version 1、事务和基本 CRUD**

实现本计划中的完整 DDL，并保证 `LibraryCatalog::open` 不创建或修改网易云数据库。所有批量更新使用一个事务；错误时回滚。

- [x] **Step 4: 增加优先级测试和实现**

覆盖：Essentia 时长优先、实测时长次之、数据库时长兜底；格式、大小、码率只允许实测覆盖数据库。

- [x] **Step 5: 运行 Catalog 测试**

Run: `cargo test --test library_catalog`

Expected: PASS。

- [ ] **Step 6: 检查变更，不提交**

Run: `git diff --stat -- src/library_catalog.rs src/lib.rs tests/library_catalog.rs`

Expected: 只出现本任务文件；等待整体验证后由用户决定是否定稿。

---

### Task 2: 跨平台发现网易云数据库与音乐目录

> **当前状态：部分完成。** 只读读取、归一化、歌曲库 snapshot、源记录和有限候选路径已实现；真实样本、完整候选路径测试、发现进度/手动兜底尚未完成。

**Files:**
- Create: `src/netease_library.rs`
- Modify: `src/netease.rs`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `discover_netease_sources(context: &PlatformPaths, progress: impl Fn(DiscoveryProgress)) -> NeteaseDiscoveryResult`。
- Produces: `read_netease_snapshot(path: &Path) -> Result<NeteaseSnapshot, LibraryError>`。
- Consumes: `LibraryCatalog::upsert_snapshot`。

- [ ] **Step 1: 写 macOS 和 Windows 候选路径测试**

数据库候选必须覆盖：

```text
macOS:
~/Library/Containers/com.netease.163music/Data/Documents/storage/sqlite_storage.sqlite3
~/Library/Application Support/Netease Cloud Music/storage/sqlite_storage.sqlite3

Windows:
%APPDATA%/Netease/CloudMusic/storage/sqlite_storage.sqlite3
%APPDATA%/NetEase/CloudMusic/storage/sqlite_storage.sqlite3
%APPDATA%/Netease Cloud Music/storage/sqlite_storage.sqlite3
%LOCALAPPDATA%/Netease/CloudMusic/storage/sqlite_storage.sqlite3
```

音乐目录优先从网易云配置或数据库中已保存的下载路径解析，其次检查有限的已知候选：macOS 的 `~/Music/网易云音乐`、`~/Music/CloudMusic`，Windows 的 `%USERPROFILE%/Music/网易云音乐`、`%USERPROFILE%/Music/CloudMusic`及客户端配置中的路径。不无界遍历整块磁盘；候选用尽后返回失败并进入手动选择。

测试通过注入临时 `home/app_data/local_app_data`，不得修改测试进程的真实 HOME。

- [ ] **Step 2: 写兼容发现进度与手动优先级测试**

断言兼容发现命令的进度事件包含 `stage/current/total/candidate`；教程完成前和之后都不由前端自动启动；若显式调用兼容命令，仍不得覆盖任务 1 当前来源。

- [ ] **Step 3: 写数据库 fixture 测试**

fixture 同时创建 `web_track`、`web_offline_track`、`web_cloud_track` 和 `track`，验证相同 `track_id` 合并为一首歌，全部原始行写入 `source_records`。

- [x] **Step 4: 实现只读数据库读取**

```rust
let connection = rusqlite::Connection::open_with_flags(
    path,
    rusqlite::OpenFlags::SQLITE_OPEN_READ_ONLY,
)?;
```

只查询存在的表；未知列保留在原始 JSON，不因网易云版本差异导致整个导入失败。

- [x] **Step 5: 实现稳定归一化规则**

合并优先级：`track_id/tid` 相同直接合并；没有 ID 时使用来源表和主键生成独立记录。多歌手按数据库数组顺序保存为 `Artist A, Artist B`。`web_track` 只有 JSON、没有离线记录时仍进入 Dashboard。

- [ ] **Step 6: 使用已知样本验证**

测试固定包含：

```text
28712318 -> FRAGILE / 山下達郎 / COZY
3409113568 -> SHE DID IT AGAIN / Tyla, Zara Larsson / A*POP
```

- [x] **Step 7: 运行测试**

Run: `cargo test --test library_catalog netease`

Expected: PASS，且 fixture 数据库保持未修改。

---

### Task 3: 解析本地文件并形成实测字段

> **当前状态：部分完成。** 基于 FFmpeg 的本地媒体探测、网易云相对路径候选和损坏/未知文件拒绝已实现；四种真实格式的完整夹具覆盖尚未完成。

**Files:**
- Create: `src/media_probe.rs`
- Modify: `src/lib.rs`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `probe_local_audio(path: &Path) -> Result<MeasuredAudioFacts, ProbeError>`。
- Produces: `MeasuredAudioFacts { format, size_bytes, duration_seconds, average_bitrate_bps, sample_rate_hz, channels }`。

- [ ] **Step 1: 先用现有测试音频验证现有库能否读取所需字段**

实测值定义：文件大小来自 `fs::metadata`；格式来自实际容器/编码头；时长来自媒体流；平均码率优先使用容器值，否则使用 `size_bytes * 8 / duration_seconds` 并标记为估算。

- [ ] **Step 2: 只有现有依赖无法稳定读取时才增加媒体探测依赖**

不得为了方便重复引入第二套标签写入库。新增依赖前记录具体失败样本，例如 VBR MP3 无法取得时长或 AIFF 无法识别编码。

- [ ] **Step 3: 写 MP3、FLAC、WAV、AIFF 与损坏文件测试**

断言格式来自内容而不是只看扩展名；损坏文件返回 `ProbeError` 并保存到 `local_files.probe_error`，不得阻止其他歌曲入库。

- [x] **Step 4: 实现本地文件候选解析**

优先使用 `web_offline_track.relative_path`；相对路径结合已检测的网易云下载目录。路径不存在时尝试数据库中 `track.file/librarypath`，仍不存在则标记 `database_only`。

- [x] **Step 5: 运行测试**

Run: `cargo test --test library_catalog media_probe`

Expected: PASS。

---

### Task 4: 增量刷新与分析缓存投影

> **当前状态：部分完成。** 本地文件增量复用、缺失标记、分析缓存投影和事务 upsert 已实现；数据库未变化时的完整快速路径和所有增量边界仍需补测。

**Files:**
- Modify: `src/library_catalog.rs`
- Modify: `src/analysis.rs`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `refresh_catalog(request, observer) -> LibraryRefreshSummary`。
- Consumes: `load_analysis_file`, `TrackAnalysis`。

- [ ] **Step 1: 写“数据库未变化不重建”的失败测试**

同一个数据库路径、大小和修改时间没有变化时复用 `catalog_sources`；仍核对已记录本地文件是否存在，但不重新解析全部原始 JSON。

- [x] **Step 2: 写文件增量测试**

本地路径、大小和修改时间都相同则复用实测字段；新增或变化文件才调用媒体探测；消失文件改为 `missing`，不删除歌曲数据库记录。

- [ ] **Step 3: 写 Essentia 投影测试**

按规范化本地路径匹配 `track-analysis.json`。分析器或模型版本变化时更新对应分析字段；Genre 写入 `essentia_genre`，不得写入 `netease_genre`。

- [x] **Step 4: 实现单事务刷新**

刷新结果先在内存构建，再以事务写入。用户取消或发生致命数据库错误时回滚；单个文件探测失败只记录该文件错误。

- [x] **Step 5: 运行测试**

Run: `cargo test --test library_catalog incremental`

Expected: PASS。

---

### Task 5: 参数化查询、筛选、排序和分页

> **当前状态：部分完成。** serde 查询接口、白名单 SQL、基础文本/数值筛选、排序和分页已有实现；计划列出的全部操作符、组合多列排序和前端兼容操作符仍未完整验证。

**Files:**
- Create: `src/library_query.rs`
- Modify: `src/library_catalog.rs`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `LibraryQuery`, `LibraryFilter`, `LibraryField`, `LibraryOperator`, `FilterLogic`, `LibrarySort`, `SortDirection`, `LibraryPage`。
- Produces: `LibraryCatalog::query(&LibraryQuery) -> Result<LibraryPage, LibraryError>`。

- [x] **Step 1: 定义 serde 接口**

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct LibraryQuery {
    pub text: String,
    pub filters: Vec<LibraryFilter>,
    pub filter_logic: FilterLogic,
    pub sorts: Vec<LibrarySort>,
    pub limit: u32,
    pub offset: u32,
}
```

`limit` 限制为 `1..=500`。字段和操作符均使用枚举，禁止前端传列名和 SQL 片段。

- [ ] **Step 2: 写每个操作符测试**

覆盖 `is`、`is_not`、`contains`、`not_contains`、`starts_with`、`ends_with`、比较、`between`、空值和布尔值；覆盖中文、日文和英文大小写。

- [ ] **Step 3: 写组合查询与多列排序测试**

示例：

```text
artists contains "Tyla"
AND effective_bitrate_bps >= 320000
ORDER BY album ASC, title DESC
```

- [x] **Step 4: 实现白名单 SQL 编译器**

所有值通过 `rusqlite::params_from_iter` 绑定；字段枚举映射到固定 SQL 表达式。非法字段、类型不匹配和不支持的操作符返回可读错误。

- [x] **Step 5: 运行测试**

Run: `cargo test --test library_catalog query`

Expected: PASS。

---

### Task 6: Tauri Catalog 状态、命令与后台进度

> **当前状态：已实现，仍需真实输出数据验收。** Dashboard 查询、详情、封面、歌词和统计只读取 `<app-data>/w4dj.sqlite3`；输出登记、分析状态更新、失效扫描/清理均绑定实际 `destination_path`。旧网易云后台刷新/手动路径命令保留在独立 `library-dashboard.sqlite3` 上作为兼容接口，但不再作为 Dashboard 数据源或按钮入口。

**Files:**
- Modify: `src-tauri/src/main.rs`
- Test: `src-tauri/src/main.rs` 内单元测试及前端服务 mock。

**Interfaces:**
- Produces commands:

```text
load_library_status
locate_netease_library
select_netease_database_fallback
refresh_library_catalog
cancel_library_refresh
query_library_catalog
get_library_track_detail
clear_library_catalog_cache
find_invalid_library_tracks
cancel_invalid_library_scan
```

- Produces event: `library-refresh-progress`。
- Produces event: `library-invalid-scan-progress`。

- [x] **Step 1: 增加 `LibraryState`**

状态集中持有 catalog 路径、手动数据库路径、刷新状态、后台取消标记和互斥刷新句柄；网易云发现结果只作为兼容刷新命令的输入，不写入 Dashboard 分析投影。不得在持有 UI controller 锁时执行 SQLite 扫描或媒体探测。

- [x] **Step 2: 写命令序列化测试**

验证查询 DTO、分页结果、详情和进度事件均使用 camelCase，前端不需要猜测字段名。

- [x] **Step 3: 实现后台刷新**

同时只允许一个刷新任务。第二次点击返回“歌曲库正在更新”。取消设置原子标记，后台在每条数据库记录和每个本地文件之间检查。

- [x] **Step 4: 实现手动定位兜底（兼容命令）**

旧接口仍支持选择 `sqlite_storage.sqlite3` 并保存数据库备用路径；当前 Dashboard 不展示或调用该入口。音乐目录继续沿用任务 1 的现有选择或拖入逻辑，不增加 Dashboard 专用目录偏好。

- [x] **Step 5: 注册命令并运行 Tauri 检查**

Run: `cargo check --manifest-path src-tauri/Cargo.toml`

Expected: PASS。

- [x] **Step 6: 接入独立输出歌曲库**

`src/w4dj_library.rs` 负责 `<app-data>/w4dj.sqlite3` 的 schema、历史一次性导入、输出文件 upsert、A/B 任务槽根目录状态、分析结果绑定、状态统计和失效扫描/清理。成功输出在安全提交后立即登记；数据库失败只产生任务警告，不删除已经成功的音频。

- [x] **Step 7: 防止兼容命令越界**

兼容网易云刷新命令写入旧 `library-dashboard.sqlite3`，Dashboard 查询、分析候选、状态和清理命令均使用 `w4dj.sqlite3`。刷新、失效扫描、重新定位和删除之间互斥，避免后台任务覆盖或删除正在检查的记录。

---

### Task 7: 教程后不自动发现与两个通用任务来源

> **当前状态：已实现，环境验收待补。** 教程完成和 WebView reload 都不会触发网易云数据库发现；用户点击任务 1 来源标题右侧的“扫描本地网易云文件夹”后，先调用自动定位并把 `musicFolder` 填入任务 1，失败时才提供手动选择兜底。已有发现进度事件映射到任务 1 进度条，高频事件只更新局部 DOM；该入口不触发歌曲库刷新。网易云数据库仅保留在转换阶段使用；真实转换环境仍需验收。

**Files:**
- Modify: `src/desktop.rs`
- Modify: `src/preferences.rs`
- Modify: `src-tauri/src/main.rs`
- Test: `tests/task_state.rs`

**Interfaces:**
- Produces: `NeteaseDiscoverySuggestion { database_path, music_folder, record_count, local_file_count }`。
- Consumes: 首次教程完成事件、Task 2 发现服务和现有任务来源选择逻辑。

- [x] **Step 1: 写教程后不启动数据库发现测试**

断言教程展示期间、教程完成后和 WebView reload 都不调用网易云数据库定位；任务 1 继续由用户选择或拖入来源。

- [x] **Step 2: 写来源选择测试**

点击任务 1 的显式扫描按钮只打开原有文件夹选择器，取消选择不改变状态，拖入和任务 2 来源不受影响。

- [x] **Step 3: 写两个输入“打开文件夹”测试**

任务 1/2 为目录时调用现有跨平台打开目录命令；为单曲时在 macOS Finder / Windows Explorer 中显示所在目录并尽可能选中该文件；空来源时禁用。

- [x] **Step 4: 保留现有任务选择逻辑**

两个任务都保留单击选择、拖入、清空、单曲/文件夹自动识别和独立输出目录。不引入 `SyncSlotRole::NeteaseLibrary` 强制角色，不屏蔽任务 1 的 `select_source_directory`。

- [x] **Step 5: 保留转换进度展示**

任务 1 进度条继续用于扫描、转换和增强分析；不显示网易云数据库发现进度，也不因教程或 reload 触发后台扫描。

- [x] **Step 6: 运行任务状态测试**

Run: `cargo test --test task_state`

Expected: PASS。

---

### Task 8: Dashboard 基础界面、事实表格与详情抽屉

> **当前状态：已实现基础能力，真实输出数据仍待验收。** Dashboard 壳层、W4DJ 输出事实表格、详情、歌词视图、状态统计和失效扫描进度已有实现；“取消更新”“选择数据库”“分析本地歌曲”三个入口已删除，网易云数据库不再参与 Dashboard 查询。歌曲行右键提供“重新定位文件”（只替换 W4DJ SQLite 的本地路径并保留分析投影）和“移除记录”（只删除 W4DJ SQLite 记录）；工具栏的“批量寻找失效歌曲”和“清除所有失效文件”分别支持后台进度/取消与勾选确认。

**Files:**
- Create: `app/src/library-dashboard.ts`
- Create: `app/src/library-dashboard.test.ts`
- Modify: `app/src/app.ts`
- Modify: `app/src/styles.css`

**Interfaces:**
- Consumes: Task 6 Tauri commands and `library-refresh-progress`。
- Produces: `renderLibraryDashboard`, `LibraryDashboardController`。

- [x] **Step 1: 写入口和空状态测试**

右上角出现“歌曲库”；点击后打开全尺寸毛玻璃弹窗。W4DJ 输出库为空时显示“还没有登记的输出歌曲”，有未分析输出时仍显示歌曲和分析状态；不得自动打开或扫描网易云数据库，也不得自动开始转换。

- [x] **Step 2: 写任务槽 UI 测试**

任务 1 的显式扫描按钮只打开文件夹选择器，仍可点击、拖入、清空和替换；任务 1/2 输入输出均保持可选择。两个输入区域都有打开文件夹按钮，且不挤压垃圾桶和路径文字。

- [x] **Step 3: 实现 Dashboard 壳层**

沿用现有主题变量、圆角、毛玻璃和弹窗关闭规则。点击背景模糊区域或按 Escape 可关闭；刷新运行时关闭只隐藏界面，不取消后台任务。

- [x] **Step 4: 实现分页事实表格**

表头 sticky；正文只渲染当前页 100 行；封面懒加载；缺失值统一显示 `—`，不得显示字符串 `null` 或 `undefined`。

- [x] **Step 5: 实现详情抽屉与歌词页签**

标准化、实测、网易云、Essentia 和原始记录分组展示。原始 JSON 默认折叠，支持复制单个字段，不提供编辑数据库按钮。歌词页签支持原文/翻译/罗马音切换、搜索、复制和下载 `.lrc`；没有歌词时显示来源缺失，不向网络自动请求。

- [x] **Step 6: 运行前端测试**

Run: `pnpm --dir app test -- --run app/src/library-dashboard.test.ts`

Expected: PASS。

---

### Task 9: 列顺序、列宽、隐藏列与筛选器

> **当前状态：已实现。** 列顺序/隐藏、列宽持久化、Shift 多列排序优先级、动态操作符、250ms 搜索防抖和过期查询响应丢弃均已接入。

**Files:**
- Modify: `app/src/library-dashboard.ts`
- Modify: `app/src/library-dashboard.test.ts`
- Modify: `app/src/styles.css`

**Interfaces:**
- Produces local preference key: `w4dj.libraryDashboard.columns.v1`。
- Consumes: `LibraryQuery` DTO。

- [x] **Step 1: 写列配置测试**

拖动表头后顺序变化并保存；刷新页面后恢复。固定 `track_key` 不显示，所有用户可见列均可隐藏，但至少保留一列。

- [x] **Step 2: 写排序测试**

单击表头依次为升序、降序、取消；按住 Shift 增加多列排序。箭头和排序优先级必须可见。

- [x] **Step 3: 写筛选器测试**

字段改变时只显示兼容操作符；数值字段不接受非数值；`between` 必须有上下界；空值操作符不显示值输入框。

- [x] **Step 4: 实现查询防抖和分页复位**

全文搜索 250ms 防抖；条件、排序或搜索变化后回到第一页；快速连续查询只应用最后一次响应。

- [x] **Step 5: 运行前端测试**

Run: `pnpm --dir app test -- --run app/src/library-dashboard.test.ts`

Expected: PASS。

---

### Task 10: “只分析不转换”工作流

本任务的分析结果更新现在绑定 `w4dj.sqlite3` 中的输出 `destination_path`；`track-analysis.json` 仅作为兼容镜像，Dashboard 不再从源路径或网易云记录枚举歌曲。缓存保存与输出回写已拆开：只有目标文件元数据事务提交并通过回读校验后，才更新 W4DJ 分析投影；普通转换覆盖输出会使旧分析失效，高级模型失败不覆盖已完成的基础分析。歌曲库工具栏可对当前输出重新分析，Reload 后可继续未完成候选。

> **当前状态：已实现。** `save_track_analyses` 只保存可复用的 `track-analysis.json` 镜像；目标文件元数据事务提交并通过回读校验后，`apply_track_analysis_results` 才按 `destination_path` 更新 W4DJ SQLite 投影。Dashboard 只展示这些结果，工具栏提供“重新分析当前输出”，不调用网易云歌曲库刷新。增强转换链路的同步 Essentia/WASM 与 TensorFlow.js 计算已移入专用 Vite Worker，主线程只保留现有音频解码/重采样和 MusiCNN 输入准备；Worker 取消会立即终止，进度更新不重建整棵应用 DOM。分析状态现在携带 `slotIndex`，候选按 `AppPreview.slot_index` 分组，任务 2 的分析进度只渲染在任务 2 卡片；Reload 后可恢复未完成候选。增强转换和 Dashboard 重分析已改为逐歌曲立即持久化输出元数据、W4DJ SQLite、兼容缓存和历史报告，批次中断不会丢弃已完成歌曲，运行会话分析报告按歌曲累积。真实长音频延迟和迁移前后数值一致性仍需人工验收。

**Files:**
- Modify: `src-tauri/src/main.rs`
- Modify: `app/src/library-dashboard.ts`
- Modify: `app/src/app.ts`
- Test: `app/src/library-dashboard.test.ts`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Consumes: 现有 Essentia 分析服务、`track-analysis.json` 和 Catalog refresh observer。
- Produces: Dashboard “刷新”行为（只查询 W4DJ 输出 SQLite）。

- [x] **Step 1: 写“不触发转换”的测试**

点击“刷新”只调用 W4DJ catalog/analysis 查询服务，不调用 `start_sync`、`start_all_sync`、预检确认或转换历史保存。

- [x] **Step 2: 写增强模式关闭测试**

关闭增强模式时不向 Dashboard 导入网易云数据库；转换阶段仍可按既有匹配链路读取网易云元数据。已有 Essentia 缓存仍可展示，但不启动新分析。

- [x] **Step 3: 写增强模式开启测试**

只分析新增、变化或缓存失效且本地可读的歌曲；分析后更新 Essentia 时长、Discogs Genre（模型可用时）、MusiCNN Style、BPM、Key、Loudness、Energy 等 W4DJ 投影。失败歌曲不进入已完成分析列表，并保留已有缓存。

- [x] **Step 4: 实现进度与取消**

进度分为数据库、文件探测和增强分析三段；取消增强分析不删除已完成结果，不生成转换文件，不写转换历史。

- [x] **Step 5: 运行相关测试**

Run: `cargo test --test library_catalog && pnpm --dir app test -- --run app/src/library-dashboard.test.ts`

Worker/回写回归：`analysis-worker-client.test.ts` 覆盖 ready/result/progress、旧 `jobId` 忽略、可转移 PCM、模型启动超时、单曲超时和立即终止；完整前端直接 Vitest 共 154 项通过（8 个测试文件），并覆盖手动错误报告/运行会话导出、取消/失败反馈、逐歌曲持久化、任务 1 自动定位网易云目录与失败后手动兜底、发现进度局部 DOM 更新、hydration 竞态与重复点击锁定、双槽分析进度路由、重新分析当前输出、Reload 恢复入口、模型维护入口移除、移除网易云 Genre/版权/发布日期表格列、Danceability 十级展示、Energy 十级阈值/格式、情绪 head 隔离和 Discogs 五 head 聚合/隔离，Vite 生产构建产出独立 `analysis.worker`。真实长音频的输入延迟、取消时序与数值对照仍需人工验收。

Expected: PASS。

---

### Task 11: 分模式元数据回写与歌词交付

> **当前状态：代码已实现，外部验收待补。** 增加 NcmCore/Enriched 元数据计划、MP3/FLAC/AIFF/WAV 字段映射和写后校验；真实格式样本与 Rekordbox 可见性仍需环境验收。

**Files:**
- Create: `src/lyrics.rs`
- Modify: `src/metadata.rs`
- Modify: `src/netease_library.rs`
- Modify: `src-tauri/src/main.rs`
- Modify: `app/src/library-dashboard.ts`
- Test: `tests/library_catalog.rs`
- Test: `app/src/library-dashboard.test.ts`

**Interfaces:**
- Produces: `MetadataWriteProfile::{NcmCore, Enriched}`。
- Produces: `build_output_metadata(profile: MetadataWriteProfile, source: &SourceMetadata, netease: Option<&NeteaseMetadataBundle>, analysis: Option<&TrackAnalysis>) -> OutputMetadataPlan`。
- Produces: `normalize_lyrics(records: &[NeteaseLyricRecord]) -> NormalizedLyrics`。
- Produces: `write_lyrics_sidecar(audio_path: &Path, lyrics: &NormalizedLyrics) -> Result<Option<PathBuf>, MetadataError>`。

- [x] **Step 1: 写普通转换标签集测试**

构造一个缺失标签的普通 MP3/FLAC，数据库匹配值包含歌曲名、两位歌手、专辑、封面、Genre、别名、版权和日期。断言 `NcmCore` 只计划写入歌曲名、完整歌手列表、专辑、封面与可用歌词，不运行 Essentia，不写曲目 ID/专辑 ID。

- [x] **Step 2: 写增强转换标签集测试**

断言 `Enriched` 在核心标签外计划写入网易云 Genre、别名、版权、发布日期和歌词，并写入 BPM、Key、Loudness、Energy、Danceability、Mood、Instrument、Drop LUFS 等现有 Essentia 结果。断言网易云 Genre 与 Essentia Genre 在 Catalog 中仍是两个字段，且身份元数据不被分析值覆盖。

- [x] **Step 3: 实现网易云有效元数据解析**

从已匹配的 `web_track`/`track`/`web_offline_track`/`web_cloud_track` 原始行中解析歌曲名、有序歌手列表、专辑、封面、Genre、别名、版权、发布日期和歌词。未知字段仍保存在 `source_records.raw_json`。曲目 ID 和专辑 ID 仅存在 Catalog 内部关联，不进入 `OutputMetadataPlan`。

- [x] **Step 4: 实现 MP3/FLAC/AIFF/WAV 映射与写后校验**

MP3 与 AIFF 使用 ID3 标准帧，FLAC 使用 Vorbis Comment 和图片块，WAV 使用 Rekordbox 官方可读的 RIFF INFO 字段。对每种格式生成 `written_fields` 与 `unsupported_fields`，写入后重新读取歌曲名、歌手、专辑、Genre、日期、版权、封面和歌词；失败只记录元数据错误，不删除已成功转换的音频。

- [x] **Step 5: 写歌词归一化与 sidecar 测试**

覆盖纯文本、标准 LRC、同一时间戳多行、翻译和罗马音；非法时间戳保留原文但标记 parse warning。输出 `.lrc` 必须与音频同名，不覆盖用户已有且内容不同的 sidecar，而是按现有冲突规则命名并在报告中记录。

- [x] **Step 6: 写 Dashboard 歌词查看测试**

断言详情抽屉可打开歌词页签，切换原文/翻译/罗马音，搜索文本，复制当前版本并下载 `.lrc`。空歌词不显示假内容。

- [ ] **Step 7: 验证 Rekordbox 可见信息**

导入 MP3、FLAC、AIFF、WAV 样本，验证歌曲名、歌手、专辑、Genre、Comments 和封面的实际可见性。歌词验收放在 W4DJ Dashboard、音频标签读回和 `.lrc`，不以 Rekordbox 当前界面显示歌词作为必须条件。

- [x] **Step 8: 运行元数据与前端测试**

Run: `cargo test --test library_catalog metadata`

Run: `pnpm --dir app test -- --run app/src/library-dashboard.test.ts`

Expected: PASS。

---

### Task 12: 缓存清理、损坏恢复和隐私边界

> **当前状态：已实现，敏感复制动作保留限制。** 私有 SQLite 损坏恢复、缓存边界和原始记录含本机路径的界面警示已有测试/实现；基于敏感路径外传限制，不启用把完整原始 JSON 复制到系统剪贴板的动作。

**Files:**
- Modify: `src/library_catalog.rs`
- Modify: `src-tauri/src/main.rs`
- Modify: `app/src/library-dashboard.ts`
- Test: `tests/library_catalog.rs`

**Interfaces:**
- Produces: `clear_library_catalog_cache`。

- [x] **Step 1: 写缓存清理测试**

清除 Dashboard 缓存只删除 `library-dashboard.sqlite3` 及其 SQLite `-wal/-shm` 伴随文件；不得删除网易云数据库、音乐文件、`scan-cache.json`、`track-analysis.json` 或模型文件。

- [x] **Step 2: 写损坏索引库恢复测试**

W4DJ 自己的索引库损坏时，将它重命名为带时间戳的 `.corrupt` 备份并重建空库；网易云原数据库损坏时只报告错误，不改名、不删除。

- [x] **Step 3: 写隐私展示测试**

详情允许显示本地路径，并在原始 JSON 折叠区提示可能包含本机路径；基于敏感路径外传限制不提供完整原始 JSON 的系统剪贴板复制，本计划不增加网络上传功能。

- [x] **Step 4: 实现并验证**

Run: `cargo test --test library_catalog cache`

Expected: PASS。

---

### Task 13: 全量回归、跨平台检查和本地 App 交付

> **当前状态：本地验收完成，外部环境项保留。** Rust/前端/格式/Tauri/Apple Silicon 验证已完成；Essentia/TensorFlow 与模型已离线内置并通过实际模型加载、零输入推理和 App 资源检查。真实歌曲、真实网易云、Windows、Rekordbox 和最终 DMG 挂载验证未完成。

**Files:**
- Modify only if tests expose defects in files already listed above.

- [x] **Step 1: Rust 全套测试**

Run: `cargo test --all`

Expected: 全部通过（本次实际根 workspace 429 项；测试数量随当前工作树新增覆盖变化）。

- [x] **Step 2: 格式与静态检查**

Run: `cargo fmt --all -- --check`

Expected: PASS。

Run: `cargo clippy --all-targets --all-features -- -D warnings`

Expected: 保留既有 legacy `dead_code` 阻断；实际新增代码已通过 `-A dead_code -D warnings` 变体。

Run: `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`

Expected: PASS。Tauri 工作区严格检查通过；根工作区严格版本仍只受既有 legacy `dead_code` 影响。

- [x] **Step 3: 前端测试与生产构建**

Run: `pnpm --dir app test -- --run`（当前环境的 pnpm 包装命令受 `ignored build scripts` 安全策略阻断；使用同一安装的 Node/Vitest 直接运行等价命令验收。）

Expected: 等价前端测试全部通过（当前 8 个测试文件、154 项；独立情绪验收工具另有 8 项）。

Run: `pnpm --dir app build`（当前环境使用同一安装的 Vite 直接运行等价构建命令验收。）

Expected: 等价 Vite 生产构建通过，并产出独立 `analysis.worker`。

- [ ] **Step 4: 使用真实网易云数据库验收转换元数据链路**

检查：转换阶段的 `FRAGILE`、`SHE DID IT AGAIN`、`offline-<track_id>.jpg` 封面、标题/歌手/专辑/歌词与 Genre 元数据匹配；确认数据库-only 记录不会进入 Dashboard，W4DJ 分析投影只由完成分析写入。

- [ ] **Step 5: Windows 逻辑验收**

至少运行 Windows target 的 `cargo check` 或 CI 编译，验证 `%APPDATA%/%LOCALAPPDATA%` 定位、路径大小写和任务 2 文件夹选择。不得仅凭 macOS 测试宣布 Windows 完成。

- [x] **Step 6: 编译 Apple Silicon 本地 App**

Run: `cargo tauri build --target aarch64-apple-darwin --bundles app`

Expected: 生成可直接打开的 `W4DJ RKB.app`，并在最终交付中提供绝对路径。

- [x] **Step 7: 展示工作树，不提交**

Run: `git status --short`

Run: `git diff --stat`

Expected: 展示所有改动，明确哪些属于本功能；等待用户说“定稿”后才允许提交和推送。

---

### 2026-08-22：增强分析稳定性与手动诊断报告

> **当前状态：自动化实现与本地验证完成，真实长音频/GUI/ExifTool 外部验收待补。** 详细执行记录见 `docs/superpowers/plans/2026-08-22-analysis-stability-and-diagnostics.md`。本阶段不修改版本号，不自动创建或覆盖用户报告，不改变 Task 6–13 接口语义。

- [x] **Task 1：显式释放 MusiCNN 逐帧对象**。以 `FrameGenerator(512, 256)` 取帧，复制 Mel 行后立即释放 Embind 对象，每 32 帧发送进度并让出 Worker 事件循环；保持 187×96 的补零和 3D Tensor 输入。
- [x] **Task 2：Worker 心跳、超时与转移缓冲**。增加 120 秒模型启动上限、按音频时长计算的 5–15 分钟单曲超时、阶段/帧计数、旧任务过滤和幂等终止；连续 PCM 缓冲直接转移，避免大音频复制。
- [x] **Task 3：逐曲 Worker 生命周期**。每首歌曲独立创建/销毁 Worker；超时记录 `timeout` 并继续，普通错误记录 `failed`，取消不写当前歌曲成功结果，已完成结果逐曲持久化，终态清理槽位和 Worker 状态。
- [x] **Task 4：转换历史与分析状态分离**。Rust 解析运行会话候选/事件/分析报告，历史卡片分别显示转换与增强分析；手动导出错误报告和运行会话记录均先请求保存路径，取消保存不调用后端。
- [x] **Task 5：完整手动诊断报告**。报告增加转换状态、增强分析总览、逐曲 pending/running/completed/failed/timeout/cancelled、Worker/阶段/耗时和运行日志；删除分析回写自动重写报告；元数据诊断分别记录源标签实际校验和文件名推断。
- [x] **Task 6：文档与验证**。前端直接 Vitest 8 文件 154/154，独立情绪验收工具 8/8，根 Rust workspace 412 项、Tauri 49 项、W4DJ manifest 集成 7 项均通过；Vite 构建、Rust fmt/check、Tauri all-targets Clippy 和 diff-check 已通过。根工作区严格 all-targets Clippy 仍被既有 dead_code 阻断。真实 9 首长音频连续分析、取消时序、Energy 阈值两侧 hover/原始筛选排序、ExifTool 标签、Windows、Rekordbox、情绪模型人工盲测和最终 DMG 挂载仍需人工/对应环境验收；情绪和 Discogs 模型资源本身已完成离线内置与校验。

### 2026-08-23：增强分析断点恢复与性能优化

> **当前状态：代码与自动化验证完成；真实长音频/WebView 后端对比及跨平台外部验收待补。** 详细执行记录见 `docs/superpowers/plans/2026-08-23-analysis-resume-and-performance.md`。不修改版本号，不提交或推送。

- [x] **Task 1–3：运行会话定位、状态持久化与 reload 恢复。** 历史保存真实会话目录并兼容旧 batch 搜索；`analysis-state.json` 原子更新逐曲状态和心跳，15 秒租约防并发并支持过期接管；重启后懒加载会话监视器。完成歌曲复用结果，未完成歌曲一首一个 Worker 继续，取消不自动恢复。
- [x] **Task 4：阶段诊断。** Worker 进度记录阶段开始/耗时、模型族/ID、backend、patch 数量和 `tf.memory()` 可用指标；手动报告显示最后心跳、当前阶段和状态快照；超时错误包含模型上下文。
- [x] **Task 5–6：MusiCNN/Discogs-EffNet 性能路径。** MusiCNN 使用连续 Float32 Mel 缓冲、64 patch 固定批次、一次双输出推理和逐批释放；Discogs 使用 `[64,128,96]` 流式批次和逐批 head 聚合，不保存全曲嵌套数组；不并行处理多首歌曲。共用 FrameGenerator 遍历和目标 WebView CPU/WebGL/WASM 实测未在当前环境完成。
- [x] **Task 7：报告与自动化验证。** 报告覆盖全部候选并将过期 running 标记为 interrupted，明确区分转换/增强分析；前端 154/154、Tauri 49/49、根 workspace 412 项、Vite、fmt、check、Tauri Clippy 和 diff-check 均通过。真实 9 首 reload/关闭/取消/继续、三次暖运行 30% 目标、Energy 阈值两侧 hover/原始筛选排序、ExifTool、Windows、Rekordbox 和最终 DMG 仍需相应环境人工验收。

### 2026-08-23：Discogs-EffNet 五 head 与独立元数据

> **当前状态：实现、离线资源校验、Rust/前端自动化验证已完成；真实长音频、ExifTool/DJ 应用和跨平台验收待补。** 详细步骤见 `docs/superpowers/plans/2026-08-23-discogs-effnet-heads.md`。本阶段不修改版本号，不提交或推送。

- [x] **Task 1：模型族注册与离线资源**。新增 `discogs_effnet_embedding`、Mood/Theme、Approachability、Instrumentation、Timbre、Discogs Danceability 六个 ID；资源脚本支持离线 staging、manifest 字节数、输入/输出节点和形状校验，旧 `discogs_effnet` 作为兼容别名保留。
- [x] **Task 2：独立 Mel 前端与共享 embedding**。Discogs 使用 `[64,128,96]` 批次、每首只执行一次 1280 维 embedding；Worker 进度携带 `modelFamily/modelId`，逐帧对象显式释放，尾部批次零填充并忽略 padding。
- [x] **Task 3：五 head 聚合**。多标签 head 使用 0.35 阈值、保留原始概率和最多 8 个标签；三套二分类 head 使用跨帧平均 Softmax 最高类别；缺失、失败、取消、超时按 head 隔离。
- [x] **Task 4：W4DJ SQLite、兼容 JSON 与音频写回**。新增五个投影列，完成 head 只更新对应列，失败/缺失保留已有值；新增 `W4DJ-Discogs-*` TXXX 命名空间并回读校验，不覆盖原始 `W4DJ-Danceability`、BPM、Key 或用户 Genre。手动错误报告新增 `[Discogs-EffNet 逐 head 状态]`。
- [x] **Task 5：Dashboard 与查询**。详情显示五个 head 的标签/类别、置信度和缺失/失败状态；五列默认隐藏但可选；查询白名单支持文本类别和 `selectedConfidence`/首个多标签置信度数值筛选。
- [x] **Task 6：进度、模型状态与报告兼容**。模型状态返回 `discogsEffnet` 子对象；旧分析 JSON 仍可读取；分析报告保留完整 `highLevel.discogsEffnet`，用户手动导出时按歌曲逐 head 展开。
- [x] **Task 7：自动化验证与文档**。本次新增前端 Discogs/shape 测试、Rust projection/metadata/report 测试；已执行 `cargo test --manifest-path src-tauri/Cargo.toml`、`cargo test --test library_catalog`、`cargo test --test w4dj_library`、`cargo test --test history`、Vite/Vitest、fmt/check/clippy（严格 root dead_code 基线按既有约定记录）。真实 9 首输出、MP3/WAV/AIFF/FLAC 外部标签和 Windows/Rekordbox 仍需人工环境。

### Task 14: Genre/Style 双轨模型与情绪字段扩展

> **当前状态：MusiCNN Style、三套情绪后台 head、Discogs-EffNet embedding、`genre_discogs400` Genre head 以及五个额外 Discogs-EffNet head 已实现并通过离线资源校验。** 本任务承接当前 MusiCNN 内置模型，但不改变基础 BPM/Key/Loudness/Energy/Danceability 算法。产品字段固定为 `Genre`（Discogs-EffNet 细分主风格）和 `Style`（MusiCNN 50 个多标签）；网易云 Genre 继续独立保存。情绪坐标与情绪簇是不同字段，不能混入现有 Mood 标签数组。五个 head 的独立合同和实现记录见 `docs/superpowers/plans/2026-08-23-discogs-effnet-heads.md`。

详细文件边界、接口、步骤和验收：`docs/superpowers/plans/2026-08-22-genre-style-emotion-models.md`。

**Files:**
- Modify: `app/src/analysis.ts`
- Modify: `app/src/analysis-worker-protocol.ts`
- Modify: `app/src/app.ts`
- Modify: `app/src/library-dashboard.ts`
- Modify: `src-tauri/src/main.rs`
- Modify: `src-tauri/src/essentia_model_import.rs`
- Modify: `scripts/prepare_essentia_tfjs_resources.py`
- Add (情绪模型资源在真实歌曲评测通过后再随包启用): `src-tauri/resources/essentia-models` 中通过严格转换校验的 Discogs-EffNet 资源和 MusicNN 情绪头资源
- Test: `app/src/analysis.test.ts`
- Test: `app/src/library-dashboard.test.ts`
- Test: `app/src/analysis-worker-client.test.ts`
- Test: `src-tauri/src/main.rs` 与 `src-tauri/src/essentia_model_import.rs` 现有测试模块

**Interfaces:**
- Produces: `HighLevelAnalysis.genre` 为 Discogs 主风格候选，`HighLevelAnalysis.style` 为 MusiCNN 50 标签候选；旧分析记录没有 Discogs 结果时 `genre` 为空，不把旧宽 Genre 冒充新 Genre。
- Produces: `HighLevelAnalysis.emotionCandidates?: { emomusic?: { model: 'emomusic', valence: number, arousal: number }, muse?: { model: 'muse', valence: number, arousal: number } }`；`moodCluster?: AnalysisLabel[]` 只在启用 MIREX 头时存在。三套新增结果先作为后台测试字段，不进入 Dashboard 默认列。
- Preserves: `HighLevelAnalysis.mood` 继续表示 aggressive/happy/relaxed/party/sad 五个独立二分类头；`danceability` 保留 Essentia 原始值和现有十级展示，模型 Danceability 若加入则单独保存概率。

- [x] **Step 1: 先写双轨字段回归测试**（MusiCNN Style 和后台情绪字段已有兼容测试；Discogs 输出夹具仍待资源）

构造一组 MusiCNN 50 标签分数，断言 `80s`、`dance`、`electronic` 等多标签进入 `style`，不得被 `deriveBroadGenreFromMsdTags` 压缩丢失；构造一组 Discogs 细分输出，断言 `Deep House` 进入 `genre`，且旧 `neteaseGenre` 不被覆盖。

- [x] **Step 2: 实现 MusiCNN Style 多标签保留**

保留已有 50 标签顺序、置信度和过滤原因；将宽 Genre 投影降级为兼容展示/迁移逻辑，新的正式 `style` 使用经过阈值筛选的多标签数组。旧缓存迁移到 `style` 时保留原值，不生成虚假的 Discogs `genre`。

- [x] **Step 3: 写三套新增情绪能力的输出类型测试**

为 `emomusic-msd-musicnn` 和 `muse-msd-musicnn` 各固定一组连续输出，验证两个模型都保留自己的 `model`、`valence`、`arousal`，范围均为 1–9，且一个模型失败不会清空另一个模型的结果；为 `moods_mirex-msd-musicnn` 固定五个情绪簇输出，验证它只写入 `moodCluster`，不写入 `emotionCandidates` 或现有 `mood` 数组；同时验证 aggressive/happy/relaxed/party/sad 五个既有二分类头仍可独立产生多个 Mood Tags。

- [x] **Step 4: 接入三套后台情绪能力，暂不公开 UI**（模型资源缺失时明确返回 `model_missing`）

在 Worker 中增加三个独立 head：`emomusic-msd-musicnn`、`muse-msd-musicnn` 和 `moods_mirex-msd-musicnn`。两套连续坐标同时写入 `emotionCandidates.emomusic` 与 `emotionCandidates.muse`，MIREX 五簇写入 `moodCluster`；每个 head 独立记录缺失、失败和耗时，不因为某一套失败而丢弃基础分析、Style 或现有五个 Mood Tags。模型资源先允许以可选导入/测试夹具存在，未完成真实歌曲评测前不新增 Dashboard 默认列、筛选项或音频标准标签回写。

- [x] **Step 5: 保留并并行运行现有五个 Mood 头**

继续加载 `mood_aggressive`、`mood_happy`、`mood_relaxed`、`mood_party`、`mood_sad`，把它们的正类置信度作为独立 `mood` 标签保存；不把它们折算为 Valence/Arousal，也不与 MIREX 五簇合并。补充测试确保五个头可以同时超过阈值，并且新三套情绪能力的缺失/取消不会覆盖既有 Mood 结果。

- [x] **Step 6: 定义 Discogs-EffNet 模型族接口**

将模型规格从固定 200 维扩展为带 `embeddingFamily`、`inputShape`、`embeddingDimensions`、`outputName` 和 `task` 的结构。MusiCNN 使用 `[patch, 96] -> 200`；Discogs-EffNet 使用官方 `[64, 128, 96] -> 1280` 形状，不能复用硬编码的 `[N, 200]` 输入。所有资源先转换为 TFJS `model.json + .bin`，重新读取并校验 manifest、层名、输入输出形状和权重长度后才可内置。

- [x] **Step 7: 接入 Discogs Genre 推理**

内置 `discogs-effnet` 与 `genre_discogs400` 头，输出前若干细分风格及置信度；主界面显示最高置信度 Genre，详情保留候选列表。Discogs 模型不可用时不回退成 Style，避免字段语义混淆。

- [ ] **Step 8: 更新 SQLite、兼容 JSON 和 Dashboard**

将索引字段改为 `netease_genre`、`genre`、`style_tags_json`，增加 emotion/moodCluster 的结构化 JSON 字段；更新查询白名单、默认列、详情抽屉、筛选和排序。旧 `essentia_genre` 数据只迁移到 Style，迁移过程不得删除或覆盖原始网易云 Genre。

- [ ] **Step 9: Worker、取消和失败回归**

确认所有模型头仍在 Worker 中执行；旧 `jobId` 消息被忽略；Discogs 模型失败时保留基础分析、Style 和已有 Genre，不写半成品。取消当前歌曲时不提交任何新的 Genre/Style/Emotion 字段。

- [x] **Step 10: 运行专项验证**（模型资源与离线加载已通过；真实歌曲长音频和主观 Genre/情绪验收待补）

Run: `pnpm --dir app test -- --run app/src/analysis.test.ts app/src/library-dashboard.test.ts app/src/analysis-worker-client.test.ts`

Run: `cargo test --manifest-path src-tauri/Cargo.toml`

Run: `cargo fmt --all -- --check`

Run: `cargo check --manifest-path src-tauri/Cargo.toml`

Expected: 旧字段兼容、双轨命名、模型结构校验、情绪输出类型、Worker 取消和 Dashboard 查询全部通过；真实歌曲需人工核对 `80s`、`Deep House`、Style 多标签和 Genre 主标签是否符合预期。

---

### Task 15：四模型情绪主观验收 HTML 工具

> **当前状态：工具实现完成，真实模型字段和人工曲库批次验收待补。** 详细实施记录见 `docs/superpowers/plans/2026-08-22-emotion-model-evaluation-html.md`；本工具不加入 Dashboard，不修改版本号，也不写回 W4DJ 数据库。

该工具固定比较四套竞争系统：现有五个 Mood 头合并成一张 Legacy Mood 基线卡、emoMusic、MuSe 和 MIREX。Rust 只读导出 `emotion-evaluation-manifest.json`，来源是 `w4dj.sqlite3` 中 `available` 的实际输出；每首记录包含相对路径、10 秒试听片段、四套模型原始状态/结果和保存的随机 seed。片段优先使用 Drop，随后用 FFmpeg 计算峰值能量窗口，无法解码时标记起点回退。

- [x] 新增 W4DJ manifest 查询、原子写入、Tauri command 和 `export_emotion_evaluation_manifest` CLI；不枚举网易云 SQL，也不修改分析缓存。
- [x] 新增可测试的随机顺序、相对路径匹配、缺失模型排除、唯一/并列/无胜者计分和 JSON/CSV 导出逻辑。
- [x] 新增工作区独立 HTML 页面：先做不看模型的主观情绪选择，再匿名显示 A/B/C/D；支持 2/3/4 方并列、暂停、IndexedDB/本地恢复和手动导出。
- [x] 完成 8 项纯逻辑/页面流程测试、Rust manifest 集成测试和 Rust/Tauri 编译检查；已生成 5/20/100 请求的 5/10/10 首 manifest。三个新增情绪 head 与 Discogs Genre 资源已随包并通过严格校验；本机静态 HTTP 服务已完成 curl 冒烟，人工浏览器验收仍需播放真实片段。
- [ ] 待 emoMusic/MuSe/MIREX 三套真实结果写入分析字段后，用 5 首、20 首、100/200 首批次完成试听选择、分母和模型映射人工验收。

### Danceability 十级展示补充

> **当前状态：实现与自动化回归完成，真实曲库外部验收待补。** 该改动只改变 Dashboard 的可见标度，不改变 Essentia 原始值、分析结果、SQLite/JSON 镜像、查询/排序、音频标签或 Energy。

- [x] 新增 `app/src/danceability-rating.ts`，固定使用 `S(x) = 1 + 9 / (1 + exp(-4.48056 * (x - 1.10370)))`，再 `round` 并限制到 1–10；null、NaN、无穷值显示缺失。
- [x] Dashboard 表格和详情改用十级显示，原始值 title 和筛选字段保留；Energy 继续使用原有数值展示。
- [x] 固定锚点和边界回归：0.8240978122→3、Joe Fight 1.1535→6、Friday Night 2.8114326→10，前端当前 7 个测试文件共 150 项通过。
- [ ] 用真实曲库核对改动前后的原始 Danceability 数值、排序和外部文件标签；当前已有 Joe Fight 镜像可读，Friday Night 不在当前真实样本中。

### Energy 十级展示补充

> **当前状态：实现与自动化验证完成，真实曲库 hover/排序人工核对待补。** 详细实施步骤见 `docs/superpowers/plans/2026-08-19-energy-ten-level-calibration.md`；数学模型、49 首人工锚点、1245 首分布和稳定性检验见 `docs/analysis/energy-rating-calibration-sop.md`。本任务只改变 Dashboard 可见标度，不改变原始 RMS² Energy、SQLite/JSON、查询/排序、音频标签或分析 Worker。

- [x] 用 49 首人工评分和 1245 首有效 Energy 完成 Huber loss + 软分布约束的十档动态规划；九个边界的分组删除最大相对移动为 1.94%，满足边界误差低于 5%。
- [x] 固定十级分界点：`0.041982342`、`0.078189338`、`0.111694549`、`0.144505646`、`0.184125843`、`0.214877708`、`0.251289228`、`0.292838207`、`0.355045821`；等于边界进入较高一级。
- [x] 保存可复现 Energy SOP、完整评分 CSV 和独立 Danceability SOP；明确只用 RMS² 时单曲人工评分误差的理论下限约 1.143 星，5% 只适用于分界点稳定性。
- [x] 按实施计划新增 `app/src/energy-rating.ts` 与回归测试，并把 Dashboard 表格和详情改用十级星标显示；tooltip、筛选和排序继续使用原始 RMS²。
- [x] 运行完整前端 154/154、Vite、Rust 412 项、fmt、check、Clippy 和 diff-check；更新 `docs/project-state.md`、`docs/handoff.md`，不提交或推送。
- [ ] 用真实曲库核对阈值两侧、hover 原始值、Energy 原始筛选/排序和 Danceability 不变；当前环境尚未执行 GUI 人工验收。

### 2026-08-23 全局扫描与 FFmpeg 并发

> **当前状态：代码与本地自动化验收完成；真实 M1 吞吐、双槽重叠和 GUI 取消仍待环境验收。** 设计记录见 `docs/superpowers/specs/2026-08-23-global-scan-ffmpeg-concurrency-design.md`。不修改版本号，不提交或推送。

- [x] 新增持久化 `concurrency_limit` 偏好，默认 2，规范化范围 1–10；滑块和数字输入双向同步，运行中修改只影响下一次任务。
- [x] 扫描使用可取消目录枚举、固定 worker、全局预算 permit、稳定索引和协调器串行合并，避免无界线程和非确定性候选。
- [x] 转换使用共享受限队列和固定 worker；FFmpeg 通过受管理 `spawn()` registry 启动，取消时停止派发、终止活跃子进程并清理未提交临时文件；两个任务槽共享同一上限。
- [x] 当前槽位显示任务启动时的并发快照；取消错误不计为歌曲损坏，已完成安全提交保留。
- [x] 完成自动化验收：根 workspace `cargo test --all` 429 项、Tauri 49 项、前端 Vitest 156 项、Vite、fmt、check、Tauri all-targets 严格 Clippy 和 diff-check 均通过；新增扫描 worker panic 可报告错误，FFmpeg registry 取消测试通过。
- [ ] 在真实 M1 批次比较并发 1/2 的 10 首吞吐，记录 CPU/磁盘瓶颈；完成双槽实际活跃数、取消 5 秒退出、GUI 响应和输出一致性人工验收。

## 2026-08-24：七项未完成计划本轮复核

本轮按 `docs/superpowers/plans/2026-08-23-w4dj-incomplete-plans-master.md` 的顺序复核了文件名安全、手动网易云数据库、FLAC 封面恢复、分析断点恢复/性能、Discogs-EffNet、Energy 和情绪验收七项计划。代码和自动化部分没有发现需要重做的已完成内容；真实数据/人工验收仍不能标记为全部完成。

- [x] 新鲜自动化复核：前端 Vitest 8 个文件、164 项通过；根 workspace `cargo test --all` 490 项通过；Tauri `cargo test --manifest-path src-tauri/Cargo.toml` 51 项通过；Vite 生产构建、`cargo fmt --all -- --check`、Tauri `cargo check`、Tauri all-targets Clippy 和 `git diff --check` 通过。
- [x] 重新构建 Apple Silicon App：`src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，arm64，SemVer `3.2.0-beta.3`，内置 Essentia/Discogs/情绪模型资源已随包。
- [ ] 真实增强分析只完成了恢复入口和进度链路核对；旧构建运行中首曲回写曾报告 `invalid args analyses for command apply_track_analysis_results`，随后已用当前源码重新构建 App。尚未把最新构建的 9 首重分析、Genre/Style/Discogs 五 head、情绪字段和 ExifTool 回读写成通过，需用户用最新 App 再点“继续未完成分析”确认。
- [ ] 89 首 FLAC/匹配 `sqlite_storage.sqlite3`、Windows、Rekordbox、DMG 挂载、Energy 阈值两侧 GUI、浏览器盲听及 100/200 首人工评测仍受素材/平台/人工操作限制。

## 2026-08-24：增强分析回写类型兼容修复

- [x] 定位并修复逐曲 `apply_track_analysis_results` 回写失败：JavaScript 的 `NaN` 在 JSON 中会变成 `null`，而 Rust 的高级分析诊断字段按 `f64` 解析，导致整首歌曲在回写前失败。
- [x] `FilteredAnalysisLabel.confidence` 改为可选数值；前端不可用置信度显式序列化为 `null`，Rust 可安全解析该诊断值，Discogs 原始分数只保留有限值。
- [x] 新增前端 26 项聚焦回归和 Rust `analysis_wire_accepts_null_unavailable_filtered_confidence` 回归；全量前端 166/166、根 workspace `cargo test --all` 490 项通过，Vite、fmt、Tauri check、Tauri Clippy 通过。
- [x] 已重新构建 arm64 App：`/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，版本 `3.2.0-beta.3`。
- [ ] 尚未把该修复写成真实 9 首高级分析通过：当前输出库仍为 6 首 completed、3 首 failed；需要在最新 App 中重新点击“继续未完成分析”，再核对 W4DJ SQLite、兼容 JSON 和 ExifTool。
- [ ] Node/FFmpeg 的单曲整套模型冒烟在 TensorFlow.js CPU backend 初始化后被环境终止，未改写任何用户数据；不替代最新 App 的 WebView/Worker 人工验收。

## 验收标准

- 网易云数据库只在转换阶段用于元数据读取/写入；Dashboard 不自动发现、打开或导入网易云数据库。
- Dashboard 只包含已经在 W4DJ SQLite 登记的实际输出文件；分析状态分别显示为未分析、失败和已完成，网易云 database-only 记录不得进入。
- 首次教程完成后仍可为任务 1 提示本地来源目录，但不会触发歌曲库刷新。
- 任务 1 和任务 2 都保留点击选择、拖入、清空和替换来源；两个输入区域都有可用的“打开文件夹”按钮。
- 第二次打开 Dashboard 只读取 W4DJ SQLite 和分析缓存，不重新扫描数据库或全量分析；新完成的分析通过投影增量更新。
- 表格支持列拖动、列宽、隐藏、分页、全文检索、结构化条件和多列排序。
- 任意已分析歌曲可查看标准化信息、实测信息和 Essentia 信息；若转换阶段带有来源记录则可查看对应原始信息。
- 文件大小、格式、平均码率遵循实测优先；时长遵循 Essentia > 实测 > 网易云；每个有效值都能说明来源。
- 网易云 Genre、W4DJ Genre 和 Style 始终分开，查询和排序也使用不同字段；80s 等 MusiCNN 标签不得因宽 Genre 投影而丢失，Discogs Deep House 等细分标签不得冒充 Style。
- 普通转换按 NCM 核心标准回写歌曲名、完整歌手、专辑和封面；增强转换额外回写可验证的数据库元数据与 Essentia 结果，但不写曲目 ID 和专辑 ID。
- 格式、码率、文件大小和时长以输出产物实测为准，只进入 Dashboard 和报告，不写成伪标签。
- Dashboard 可查看、搜索、复制和导出歌词；MP3/FLAC/AIFF 按支持能力写入标准歌词标签，有时间轴时可产生同名 `.lrc`，不要求 Rekordbox 展示内嵌歌词。
- 未完成分析的输出歌曲仍可在 Dashboard 统计和状态中看到，不会伪装为已完成分析；一个空的 W4DJ 输出库不会阻塞任务 2。
- 刷新 Dashboard 只重新查询 W4DJ SQLite，不产生转换文件或转换历史。
- 清除 Dashboard 缓存不会影响网易云数据库、分析缓存、扫描缓存、模型或音乐文件。

## 实施顺序建议

先完成 Tasks 1–5，得到可测试的无界面 Catalog；再完成 Tasks 6–7 接入 Tauri 和通用任务来源；然后完成 Tasks 8–10 的 Dashboard 与分析交互，Task 11 负责元数据和歌词交付，Task 12 负责缓存与恢复。Task 13 只做回归和交付，不在该阶段追加新需求。

---

## 2026-08-23：FLAC 封面数据库恢复（代码已实施）

> **当前状态：Task 1–6 的代码、诊断、单元测试和 Tauri 接线已完成；Task 7 的完整自动化验证正在执行，89 首真实 FLAC/GUI 验收受当前环境数据与人工操作限制。** 唯一实施规范为 `docs/superpowers/plans/2026-08-23-flac-cover-database-recovery.md`；本轮不改版本号、不提交、不推送。

- [x] 显式解析并在整个批次共享同一个只读网易云元数据解析器，手动数据库优先，失效时自动回退并报警。
- [x] 为普通 FLAC 转换、“仅更新元数据”、重试和恢复路径补齐保守匹配、本地封面恢复和写后校验；已有有效封面不得覆盖，远程 `picUrl` 仅报告为 `remoteOnly`。
- [x] 手动导出的运行会话 JSON 和错误报告能区分数据库不可用、无匹配、歧义、本地缓存缺失、图片无效及输出验证失败。
- [ ] 完成全部命令的最终验证和 89 首 FLAC 真实数据验收；代码已提供只读数据库/WAL/SHM 核对所需的单元测试与报告字段，实际用户批次仍待可读数据和人工 GUI 环境。

实现记录：`NeteaseMetadataResolver` 在批次开始加载一次并通过 `ConversionMetadataContext` 传入普通转换、元数据更新、重试和恢复路径；`MetadataDiagnostic.netease_recovery` 与运行会话事件记录数据库路径、记录数、匹配方法、封面来源和终止原因。封面恢复保持“嵌入 > SQLite blob > 本地引用 > 本地缓存 > remoteOnly/missing”优先级，不访问远程 URL；错误报告新增 `[网易云数据库与封面恢复]` 汇总。新增 resolver、只读快照、远程 URL 隔离、元数据更新封面回写和报告覆盖测试。

## 2026-08-23：手动选择网易云元数据数据库（代码已实施）

> **当前状态：Tasks 1–5 代码和自动化验收完成；真实数据库/音频 ExifTool 与 GUI 验收待实际素材。** 计划见 `docs/superpowers/plans/2026-08-23-manual-netease-metadata-database.md`；不改版本号、不提交、不推送。

- [x] 任务 1 来源标题右侧提供“选择网易云数据库”和“恢复自动定位”，选择/清除不触发扫描、转换、分析或 Dashboard 刷新。
- [x] 手动路径严格 read-only 校验支持的网易云 schema；无效路径和保存失败都保留旧偏好，状态载荷使用 camelCase。
- [x] 普通转换、仅更新元数据和分析后置写回共享批次级不可变 resolver；Dashboard 继续只查询 `w4dj.sqlite3`。
- [x] 前端测试覆盖文件选择取消、无效选择、成功选择无副作用、恢复自动定位、重复点击和任务 1 独占渲染。
- [ ] 使用用户真实 `sqlite_storage.sqlite3` 与 FLAC/MP3 执行 ExifTool 写后回读、WAL/SHM 时间戳对照和 GUI 验收。

## 2026-08-23：未完成计划统筹元计划

统一执行入口为 `docs/superpowers/plans/2026-08-23-w4dj-incomplete-plans-master.md`。阶段 0 状态核对已完成；阶段 1 文件名与冲突安全已完成代码与聚焦自动化，阶段 2 手动网易云数据库代码与自动化复核已完成。当前阶段 3 受现场素材限制：迁移快照虽有支持表但 `track` 为 0 行，当前没有可匹配的 `sqlite_storage.sqlite3` 与 89 首 FLAC，因此只完成只读 schema、WAL/SHM 和现有少量 FLAC/MP3 ExifTool 核对，未把它们写成 89 首真实验收通过。

阶段 1 自动化记录：filename policy 4 项、sync policy 112 项、preview 18 项、sync 39 项、history 12 项、NetEase 30 项、preferences 5 项和 Tauri 51 项通过；相关 Rust 文件 rustfmt 检查通过。后续顺序固定为：真实数据库/FLAC 验收 → 增强分析断点恢复与性能、Discogs、Energy、情绪模型真实验收 → Task 11/13 外部验收和最终 App 构建。缺少 Windows、Rekordbox、89 首素材或人工 GUI 时只记录环境限制，不伪造通过。

本轮统筹验收补充：前端 jsdom 164/164、根 workspace `cargo test --all`、Tauri 51/51、Vite、Tauri check/Clippy、Rust fmt 和 diff-check 均通过。当前 `w4dj.sqlite3` 有 9 条分析结果（6 完成、3 失败），高级 `genre/discogsEffnet/style` 尚未因真实重分析而产生；最新 App 已构建到 `/private/tmp/w4dj-tauri-build/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，arm64、版本 `3.2.0-beta.3`。89 首 FLAC/匹配数据库、真实长音频恢复、人工 GUI、Windows、Rekordbox、DMG 和浏览器盲听仍受环境限制。

---

## 独立未来功能：W4DJ × DJ Crate Digger

> **当前状态：待确认、未启动。** 本功能不属于上方 Dashboard Task 1–13，不改变其实施顺序。需求来源见 `docs/W4DJ-dj-crate-digger-handoff.md`。只有用户明确要求启动后，才为各子系统编写可执行实施计划；当前不得据此修改产品源码或版本号。

- [ ] **规划确认：冻结输入与冲突规则。** 确认 `.w4djcrate` 完整 schema、向后兼容规则、专辑不同但录音相同的判定、Remix/Edit/Live 冲突策略、占位 MP3 的固定来源与时长、永久目录布局、编号规则及默认分段数。
- [ ] **歌单导入与本地匹配。** 保留原始顺序，复用已有曲库，为每首歌给出“已有、待获取、版本冲突、无法匹配”等可复核状态；内部 ID、平台引用和去重键不得写入最终音频标签。
- [ ] **Rekordbox 准备包。** 完成跨平台文件名清理、顺序编号、重名处理、合法 pending MP3 占位文件及相对路径 M3U8，确保每个条目都指向实际音频文件。
- [ ] **局域网手机页面。** 提供带随机临时令牌的本地页面和二维码，按原顺序分段复制“歌名 艺人”；覆盖端口占用、不同 Wi-Fi、不可达和令牌过期，并避免在 URL、日志或报告中泄露完整歌单及不必要的本地信息。
- [ ] **下载回收与安全替换。** 扫描或监听网易云下载目录和统一收件箱，处理 NCM 与常见音频格式；用临时文件和安全提交替换 pending 文件，失败时保留原文件，并保存匹配证据与冲突记录。
- [ ] **跨平台与 Rekordbox 验收。** 使用 10、20、50 首歌验证二维码分段、顺序、最终 M3U8 和实际文件逐条一致；在 macOS/Windows 实测相对路径、占位导入、真实文件覆盖及 Rekordbox 标签/时长/波形/BPM/调的刷新行为。

MVP 不接入 Spotify 或 Apple Music API，不上传云端，不保存网易云账号或 Cookie，也不依赖非官方网易云网络 API。

## 2026-08-24：MusiCNN 长音频内存边界修复与最终构建复核

- [x] 复现并定位旧实现的现场问题：`FrameGenerator` 加整首歌曲共享 Essentia 实例在 macOS WebView 中分析 `Hallelujah` 的 17,346 帧时，WebContent RSS 增长到约 1.6–2.4 GB，进程重建后又回到首曲；这不是数据库回写失败，而是逐帧原生分配的生命周期边界不足。
- [x] `computeMusiCnnMelRows` 在生产 Essentia runtime 中按 256 帧分块创建短生命周期实例；每块结束释放 FrameGenerator 并销毁实例，继续保持 512/256 帧参数、原始顺序、187×96 补零和每 32 帧进度。测试替身保留单实例兼容路径。
- [x] 新增长信号分块、实例创建/销毁和行顺序回归测试；前端完整 Vitest 8 文件 173 项通过，TypeScript 检查通过。
- [x] 根 workspace `cargo test --all` 490 项、Tauri 52 项、`cargo fmt --all -- --check`、Tauri `cargo check`、Tauri 严格 Clippy、根 `-A dead_code -D warnings` Clippy、Vite 和 `git diff --check` 均通过。
- [x] 最终正常前端入口已清理临时验收脚本，Apple Silicon App 已重新构建：`/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，版本 `3.2.0-beta.3`。
- [ ] 新分块实现尚未在最新版 App 中完成当前歌曲库的真实长音频全批次回写；本轮只对旧构建完成了后台 `open -g` 现场复现，自动审查不允许用临时无条件脚本触发用户数据库写回。需用户在后台启动的最新 App 中手动点击“重新分析当前输出”，再核对 W4DJ SQLite、兼容 JSON、ExifTool 和取消/恢复时序。

## 2026-08-24：模型 Worker 二进制传输与旧 Mood 输出节点修复

- [x] 修复 Essentia 模型权重在 Worker 启动前被展开为超大 `number[]` 导致 `loadingModels` 长时间无 `ready` 的问题；Tauri JSON/Worker 二进制两条路径现在统一使用可转移 `ArrayBuffer`，Worker 反序列化保留 `Uint8Array`。
- [x] 修复内置 `mood_aggressive`、`mood_happy`、`mood_relaxed`、`mood_party`、`mood_sad` 和 `voice_instrumental` 运行时规格错误使用 `model/Sigmoid` 的问题；随包图实际输出为 `model/Softmax`，新增资源节点回归测试防止再次漂移。
- [x] Chromium Worker 烟测：30 秒真实 MP3 临时片段完整 17 模型链完成；MusiCNN 基础分析、Discogs Genre、五个 Discogs head、五个旧 Mood/Voice、emoMusic、MuSe、MIREX 均返回终态，未写入用户数据库或音频。
- [x] 长音频复核：真实 `I Feel Love` 在 Chromium Worker 中完成完整 17 模型链，基础分析、Genre、五个 Discogs head、五个旧 Mood/Voice、emoMusic、MuSe、MIREX 均成功，耗时约 10 分钟；结果仅用于验收，不写用户数据库或音频。
- [x] 最终本机验证：前端 Vitest 167/167、根 workspace `cargo test --all` 490 项、Tauri 52 项、Vite、fmt、Tauri check、`-A dead_code -D warnings` Clippy 和 diff-check 通过；严格根 all-targets Clippy 仍保留既有 dead-code 基线。
- [ ] 真实 9 首输出的逐曲回写、W4DJ SQLite/兼容 JSON/ExifTool 交叉核对仍需使用最新版 App 完成；89 首 FLAC/真实网易云数据库、Windows、Rekordbox、Energy GUI、浏览器盲听及 100/200 首人工评测仍按原环境限制保留。

## 2026-08-24：后台启动验收与 MusiCNN 常量作用域复核

- [x] 按要求只使用 `open -g` 后台启动最新 App；进程保持运行，未调用普通 `open -a`、`activate` 或 `AXRaise`。
- [x] 发现 `app/src/analysis.ts` 同时声明导出的 `MUSICCNN_INFERENCE_BATCH_SIZE` 和内部同名常量；已删除重复内部声明，统一使用导出常量，避免 Worker 打包后出现未解析的批次常量。
- [x] 当前前端 Vitest 8 个文件、170 项通过；根 workspace `cargo test --all`、Tauri 52 项、fmt、Tauri check、Tauri all-targets Clippy、根 Clippy（`-A dead_code -D warnings`）、Vite 和 `git diff --check` 均通过。
- [x] 计划要求的根 workspace 严格 `cargo clippy --all-targets --all-features -- -D warnings` 已重跑；仍仅因既有 `src/sync.rs:3378 paths_refer_to_same_file` 的 `dead_code` 失败，未出现本轮新增警告。
- [x] 已重新构建 Vite 和 Apple Silicon App；版本 `3.2.0-beta.3`，产物为 `/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`。
- [x] 只读核对当前 `w4dj.sqlite3`：9 条 `/Users/mac2/Music/test` 输出记录，`analysis_results` 为 7 条 `completed`、2 条 `failed`；其中只有 Hallelujah 的 `highLevel.status=completed`，其余旧记录的高层失败原因包含 `Can't find variable: MUSICCNN_INFERENCE_BATCH_SIZE`。这是历史运行状态，不能当作本次最新 App 重分析通过。
- [ ] 仍需在最新 App 中由用户手动点击“继续未完成分析/重新分析当前输出”，再交叉核对 9 首的 W4DJ SQLite、兼容 JSON、Genre/Style/Discogs/情绪字段及 ExifTool 写回；后台启动本身不会自动开始分析。
### 2026-08-24：整库重分析授权现场结果

- [x] 按用户授权以 `open -g` 启动并触发一次 Dashboard 重分析；只读检查进程、SQLite 和文件 mtime。
- [x] 发现当前“重新分析当前输出”按任务槽输出根目录过滤，本次实际候选为 9 首 `/Users/mac2/Music/test`，不是独立 W4DJ 库的 16 首。
- [x] 无存活 `w4dj-desktop` 分析进程，连续 5 秒无新的 `w4dj.sqlite3`/`track-analysis.json` 写入；保留原有结果，不删除数据库或 JSON。当前仍为 `tracks=16`、`analysis_results=8 completed/1 failed`。
- [ ] 真实 16 首整库高级回写、SQLite/JSON/ExifTool 交叉核对仍未完成；下一步需先处理候选范围与长音频解码超时后的恢复策略。

### 2026-08-24：整库重分析候选范围修复

- [x] `list_library_analysis_candidates` 现在以 `w4dj.sqlite3` 的 `available` 且可读本地文件为唯一候选来源，不再按当前任务槽输出根目录过滤。
- [x] 当前输出根目录仅用于保留任务槽标记；不在当前偏好目录下的有效 W4DJ 输出也会进入“重新分析当前输出”队列。
- [x] 新增 Rust 回归测试，覆盖配置根目录外的 FLAC 仍被纳入、非音频/内部临时文件仍被排除。
- [x] 前端 Vitest 全量 173/173、Tauri 候选回归测试、`cargo check`、Vite 生产构建和 arm64 App 打包均通过。
- [ ] 尚未在最新版 App 中启动新的 16 首真实整库分析；需后续按 `open -g` 规则运行并核对 SQLite、兼容 JSON 与 ExifTool。

### 2026-08-24：后续验收改为无 GUI 后台模式

- [x] 后续验收规范改由 `docs/superpowers/plans/2026-08-24-headless-acceptance.md` 统一管理；既有历史记录不改写。
- [ ] 实施隐藏验收运行时和共享歌曲库分析 runner；验收不得打开 W4DJ App GUI，不再使用 `open -g` 后台按钮、可访问性点击、`activate`、`AXRaise` 或截图。
- [ ] 使用 `--headless-acceptance libraryAnalysis --exercise-cancel-resume` 完成 16 首真实整库分析，并以 JSONL、进程、SQLite、兼容 JSON、mtime 和 ExifTool 验收。

## 2026-08-25：W4DJ 歌单导入、二维码与相对路径 M3U8

实施规范：`docs/superpowers/plans/2026-08-24-w4dj-playlist-qr-import.md`。

- [x] `.w4dj` v1 文件解析、大小/字段/控制字符/重复去重校验，及 `w4dj.sqlite3` schema v2 歌单持久化。
- [x] Tauri 导入、列表/加载、UTF-8 TXT 导出命令；导入不读取网易云数据库，也不改变音频、历史或分析 JSON。
- [x] 前端本地 QR 分页（每页最多 40 首、1500 UTF-8 字节、M 纠错）、复制当前页/全部、显式 TXT 导出和整窗模糊拖入覆盖层。
- [x] W4DJ 独立输出库候选匹配：只使用 `available` 且可读的当前输出，精确匹配、歧义/未匹配报告、手动覆盖和 `track_key` 持久化；失效输出显示 `missing`。
- [x] Tauri 一键识别、手动复核、清除覆盖和相对路径扩展 M3U8 原子导出；完整导出与明确确认的部分导出分开。
- [ ] 真实 Afro House `.w4dj` 手机扫码/网易云粘贴、真实转换输出 10/10 匹配、M3U8 播放器/Rekordbox 导入仍待对应设备/素材；本轮只完成代码与本地自动化验证。
- [ ] 后续 89 首 FLAC/网易云数据库、四格式、Energy、Emotion、Windows 和 DMG 均迁移到后台场景；主观盲听和 Rekordbox 实机导入保留为独立外部人工限制，不要求打开 W4DJ App GUI。

### 2026-08-25 收尾验证

- [x] 前端 Vitest 全量 26 个文件、388 项通过（含 100 首 QR 分页夹具）；TypeScript 检查和 Vite 生产构建通过。
- [x] 歌单匹配、独立库持久化、M3U8 针对性 Rust 测试分别通过 5/5、7/7、4/4；Tauri 测试 57/57 通过。
- [x] Apple Silicon App 已用 `cargo tauri build --target aarch64-apple-darwin --bundles app` 重新构建于 2026-08-25 01:00:33，版本保持 `3.2.0-beta.3`，并包含 FFmpeg sidecar。
- [x] 根 `cargo test --all`、`cargo fmt --all -- --check`、Tauri `cargo check`、严格 Tauri Clippy 和 `git diff --check` 均通过。DMG 脚本/`hdiutil` 在当前环境因参数/设备配置失败，未生成新的可挂载 DMG。
- [x] 收尾修正 Tauri 歌单导出路径的 Clippy `collapsible_if` 与未使用导入；`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` 和 Tauri 57 项测试再次通过。全仓 `cargo fmt --all -- --check` 仍会报告工作树既有大范围未格式化差异，本轮未格式化无关文件。
- [x] 最终复跑 `cargo test --all` 退出码为 0，当前根 workspace 全量测试通过；之前 3 个 filename fallback 失败未能在本次复跑中重现，仍保留历史记录以便追踪。`cargo fmt --all -- --check` 的既有跨文件格式差异仍未批量改写。

- [ ] 外部人工验收仍未执行：手机逐页扫码并粘贴 NetEase、在 IINA/VLC 播放导出的 M3U8、在 Rekordbox 导入并核对顺序。完成顺序固定为“扫描每页并比对文本 → 粘贴全部文本 → 播放器打开 M3U8 核对 10 首 → Rekordbox 导入核对 10 首”；不把这些环境限制写成通过。

### 2026-08-24 无 GUI 后台验收执行结果

- [x] 隐藏 `headless.html`、`--headless-acceptance` 参数、JSONL 事件和共享分析 runner 已实施；未使用 GUI、可访问性点击或截图。
- [x] 最新 arm64 App 已重新构建，版本保持 `3.2.0-beta.3`：`/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`。
- [ ] 16 首整库真实分析未完成：三次 `libraryAnalysis --exercise-cancel-resume` 在首曲 MusiCNN 提取接近 17,344 帧时 WebContent 重启，JSONL `/private/tmp/w4dj-headless-acceptance/library-analysis-16-final.jsonl` 出现 3 个 runId，无持久化事件。已正常停止 App，SQLite/JSON 保持 `8 completed / 1 failed / 7 notAnalyzed`。
- [ ] 取消/恢复、16 首 SQLite/JSON/ExifTool 交叉核对，以及后续 FLAC/数据库后台场景待隐藏 WebContent 稳定性修复后继续。

### 2026-08-25：高级模型懒加载

- [x] 普通启动不再扫描、校验或复制高级模型。
- [x] 首次增强分析通过 `ensure_essentia_models` 懒校验/安装模型，再加载到 Worker；隐藏分析 runner 使用同一路径。
- [x] 前端 Vitest 191/191、TypeScript、Vite、Tauri 57/57、`cargo check`、严格 Tauri Clippy、fmt 和 arm64 App 构建通过。
- [x] 最新 App：`/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，版本 `3.2.0-beta.3`。

## 2026-08-25：快速数据库扫描与转换后增强分析

实施计划：`docs/superpowers/plans/2026-08-25-fast-database-scan-and-preconversion-analysis.md`。

- [x] Task 1 已落地：`probe_netease_database` 只读探测支持表和行数，不解析 `web_track.track` JSON；`load_records_from_db_observed` 以有界并发读取支持表，并通过进程内 fingerprint 缓存复用 `Arc<Vec<NeteaseRecord>>`。
- [x] 自动/手动发现改为“先探测、后加载”：候选数据库先做轻量 schema/计数探测，只加载记录数最多的支持库；手动路径 schema 无效时立即回退自动发现。
- [x] 任务 1 的网易云发现改为“先返回路径，后台补计数”：`locate_netease_library(true)` 先返回数据库/音乐目录，随后通过 `netease-discovery-progress` 的 `checkingMusicFolder`/`completed` 事件补发本地文件计数；已选任务 1 来源与发现目录相同时不重复计数。
- [x] Task 2 现状已收口：输入目录单次枚举、扫描缓存根目录规范化和扫描/FFmpeg 并发隔离保留；`enumerate_music_files_observed` 回归测试通过。
- [x] Task 3/4 现状已收口：增强分析默认关闭；普通流程保持“扫描 → 数据库准备 → 转换”，只有显式开启增强模式或歌曲库手动重分析时才进入“转换后增强分析”。数据库阶段与增强分析阶段都映射到任务卡进度条，前端高频事件只做局部 DOM 更新。
- [x] 自动化验证通过：`cargo test --test netease` 4/4、`cargo test --test sync_policy` 115/115、`cargo test --test library_catalog` 18/18、`cargo test --manifest-path src-tauri/Cargo.toml` 57/57、`cargo fmt --all -- --check`、`cargo check --manifest-path src-tauri/Cargo.toml`、`cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`、前端 Vitest 12 文件 191/191、Vite 生产构建、`git diff --check` 全部通过。
- [x] 本机真实只读证据：`/Users/mac2/Library/Containers/com.netease.163music/Data/Documents/storage/sqlite_storage.sqlite3` 可只读打开，支持表为 `track` 和 `web_track`，当前计数分别为 `0` 和 `538`；探测前后 SQLite `mtime` 未变化。
- [ ] 计划中的“当前用户外置 T7 数据库 + 2,398 输入文件”真实时序验收未在本轮执行。当前环境只核对到了本机默认容器库，没有把外置卷 I/O、完整扫描耗时和转换开始时间写成已验收通过。

## 2026-08-25：快速数据库扫描与转换后增强分析

实施规范：`docs/superpowers/plans/2026-08-25-fast-database-scan-and-preconversion-analysis.md`。

- [x] 网易云数据库增加只读 schema/计数探测、最多 2 个表读取 worker、进程内 fingerprint 缓存；自动候选先探测再读取，手动数据库仍优先且无效时回退。
- [x] 网易云目录计数继续在后台阶段事件中完成；任务 1 来源目录与发现目录规范化相同不会重复计数。
- [x] 扫描 worker 使用有界 scan-only worker 集合，不再占用 FFmpeg permit；为显示精确分母，任务开始增加可取消的轻量预枚举，正式扫描 worker 对每个文件只处理一次，取消和阶段进度保持可见。
- [x] 增强分析保持默认关闭，严格在转换批次完成后逐曲 Worker 分析；写回独立显示 `writingBack`，失败/取消不阻塞其余歌曲且结果绑定实际输出路径。
- [x] 聚焦自动化：前端 app/worker 测试 125/125，Rust `netease` 4/4、`sync_policy` 115/115、Tauri 57/57 通过。
- [ ] 真实 T7 2,398 文件性能、数据库路径返回耗时、取消和 SQLite/JSON/ExifTool 交叉验收尚未执行；不把环境外验收写成通过。

### 2026-08-25：扫描进度显示修复

- [x] 扫描任务按输入目录和输出目录分别累计 `processed/total`，不再把两个目录的统计混用。
- [x] 总数未知时显示“正在扫描输入/输出目录 · 已扫描 N 项”，并使用不定长进度条；总数确定后才显示 `N/总数`。
- [x] 扫描期间任务卡状态显示“运行中”，不再显示“待命”；转换取消按钮仍只对应实际转换任务。
- [x] 前端全量 Vitest 392/392、TypeScript、Vite、Tauri 57/57、`sync_policy` 117/117、Tauri check 和严格 Clippy 通过；arm64 App 已重新构建，版本保持 `3.2.0-beta.3`。
- [ ] 全仓 `cargo fmt --all -- --check` 仍报告工作树既有的 `src/netease.rs`/`src/netease_cache.rs` 格式差异，本轮未批量改写无关文件。
## 2026-08-25 轻量网易云索引与快速启动（已实施代码阶段）

- [x] 启动只做网易云 SQLite schema/计数探测和 locator 缓存摘要读取，不再解析完整歌曲 JSON。
- [x] 在现有 `library-dashboard.sqlite3` 增加轻量 locator 表；只保存 track ID、来源表/rowid、fingerprint、路径/文件名、大小和最小匹配键，不保存完整元数据、歌词或封面。
- [x] 增加后台缓存构建、进度事件、单例和协作取消；源数据库保持 read-only，fingerprint 变化时旧快照不被覆盖。
- [x] 扫描/转换首次触达元数据边界时准备缓存；歌曲匹配后按 rowid 单曲读取完整元数据，普通无网易云数据库的扫描继续可用。
- [x] 新增 locator round-trip、无 raw metadata 列、WAL/SHM 失效和源数据库只读回归测试；前端扫描取消会同步请求缓存取消。
- [x] 已重新构建最新版 arm64 App：`/Users/mac2/Documents/W4DJ RKB/src-tauri/target/aarch64-apple-darwin/release/bundle/macos/W4DJ RKB.app`，版本 `3.2.0-beta.3`。
- [ ] 外置 T7/2,398 文件冷启动、扫描耗时、真实元数据写回和 SQLite/JSON/ExifTool 现场验收仍待执行。

## 2026-08-25：任务 1 网易云身份命名与缺失标签回退

实施交接：`/private/tmp/W4DJ-task1-netease-db-filename-metadata-handoff.md`。

- [x] 任务 1 的 `PreserveSource` 路由在生成预览目标路径前查询只读 `NeteaseMetadataResolver`；匹配成功时按数据库 Title/Artist 和用户选择的 `TitleArtist`/`ArtistTitle` 规则生成一次路径，`Original` 仍逐字符保留源 basename。
- [x] 任务 1 候选保存数据库 Title/Artist/Album 与 track/album ID，供重复路径消歧和后续登记复用；路径确定后元数据、封面、分析写回不重新计算路径。
- [x] 无标签 FLAC/MP3 的任务 1 元数据写回使用同一 resolver 只补缺失字段；任务 2 `SoundCloud` 路由保持源标签/文件名回退，不启用网易云数据库补写。
- [x] 修正全角引号/弯引号匹配、仅剥离已知音频扩展名的文件名 stem，以及 `web_track.track` 无本地路径时的惰性 locator 文件名键；解决 `Mass Destruction (＂P3＂ + ＂P3F＂ ver.) - 川村ゆみ,Lotus Juice` 无法匹配的问题。
- [x] 回归验证覆盖 resolver-aware 预览、无标签 MP3 写回、任务 2 隔离、惰性 locator round-trip、全角标点真实身份匹配；真实源文件和网易云数据库仅只读检查，未改音频、数据库或既有输出。
- [x] 使用当前真实网易云容器库与 T7 上的 `Mass Destruction` 无标签 FLAC，在临时输出目录完成任务 1 预览路径和 Title/Artist/Album 写后读回；源文件内容保持不变。
- [ ] 当前环境未执行真实 FLAC/MP3 重新转换、ExifTool 写后回读及外置 T7 全批次验收；需用户在最新版 App 中重新执行任务 1 后再验收实际输出路径与标签。

## 2026-08-25：W4DJ 最小歌单格式 v2 与网易云 ID 匹配（已完成代码与自动化验收）

实施计划：`docs/superpowers/plans/2026-08-25-minimal-w4dj-playlist-format.md`。

- [x] 新 `.w4dj` 固定为 `format_version=2`，只接受协议字段、`playlist.name` 以及歌曲 `position/title/artist_display/netease_track_id`；旧 v1 和旧字段 fail-closed 拒绝，不做迁移或兼容读取。
- [x] 网易云歌曲 ID 始终按 JSON 字符串保留；没有 ID 时省略字段，内部 dedupe key 和网易云导入文本由程序派生，不写入交换文件。
- [x] `w4dj.sqlite3` 的实际输出身份映射优先按 `netease_track_id` 命中；只有缺少 ID 时才进行唯一标题+歌手回退，歧义必须手动确认；Dashboard/M3U8 不读取网易云 SQL 枚举歌曲。
- [x] 转换安全提交后将 ID 与最终 `destination_path` 一起登记；W4DJ v2 导出通过用户选择保存路径手动生成，不自动覆盖旧报告或旧歌单。
- [x] UI 显示 ID、精确匹配/标题歌手回退/未匹配/需要确认状态，并保留重复 position；M3U8 按原始 position 输出重复播放行。
- [x] 只读核对 `/Users/mac2/Documents/Codex/uk-bass-simulated-10.w4dj` 为 v1 且包含旧字段；解析器拒绝并提示重新导出 v2。生产测试覆盖 18 位 ID、非字符串 ID、旧字段、ID 精确匹配、相似标题不回退、歧义和重复播放位置。
- [x] 自动化验收：根 `cargo test --all`、Tauri 57 项测试、前端 Vitest 192 项、TypeScript、Vite、Tauri check、严格 Tauri Clippy、Rust fmt 和 `git diff --check` 均通过。
- [ ] 手机扫码/网易云粘贴、外部播放器播放 M3U8、Rekordbox 实机导入和未挂载外置卷仍属于环境外人工验收，不能由本轮自动化替代。

## 2026-08-25：扫描取消即时响应与动态总数修复（本轮完成）

- [x] 扫描任务在输入预枚举、输入/输出扫描、网易云元数据匹配各阶段共享取消标记；`cancel_scan` 立即返回 `cancelling`，后台线程逐条检查并以 `cancelled` 终态收口，重复取消保持幂等，不进入转换。
- [x] 扫描开始前以可取消的目录枚举确定输入/输出总数，任务卡从 `0/总数` 开始累加；元数据阶段使用独立 `metadata_processed/metadata_total`，不再把最终 `1088` 误显示成单独的“已扫描 1088 项”。
- [x] 网易云轻量 locator 使用路径、文件名和 stem 索引，匹配期间不逐候选打开完整 SQLite，也不在扫描开始调用 `load_exact`；每个候选检查取消标记。
- [x] 前端扫描轮询改为局部更新任务卡文本和进度条，运行中的高频事件不再重建整棵 DOM；取消按钮先显示“正在取消扫描”，终态才恢复完整渲染。
- [x] 真实 T7 输入目录只读验收：1088 首候选、1088 个元数据阶段事件，轻量索引预览耗时 3.52 秒；未修改源数据库、音频或用户分析数据。
- [x] 自动化验收：前端 Vitest 194/194、TypeScript、Vite 构建、根 `cargo test --all`、Tauri 58/58、`cargo check --manifest-path src-tauri/Cargo.toml`、Tauri 严格 Clippy、Rust fmt 和 `git diff --check` 通过。
- [ ] 根 workspace `cargo clippy --all-targets -- -D warnings` 仍被既有全 target `dead_code` 与 `tests/duplicate_track_acceptance.rs` 的 `map_identity` 阻断；本轮新增扫描代码的 Clippy 问题已修复。真实 GUI 点击取消、Windows/Rekordbox 和未挂载外置卷仍需现场验收。

## 2026-08-27：Discogs EffNet 内置资源去重

- [x] App 仅内置正式 `discogs_effnet_embedding.{json,bin}`，移除内容完全相同的旧 `discogs_effnet.{json,bin}` 副本。
- [x] 保留旧 ID 的用户模型导入、状态回退和读取兼容；新增 canonical-only 资源断言与旧 ID 导入回归测试。
- [x] 离线生成脚本停止复制旧副本，资源说明更新为“旧 ID 仅用于兼容导入”。
- [x] 资源约减少 17.4 MiB，最新版 arm64 App 约 95.5 MiB，版本保持 `3.2.0-beta.3`。
- [x] Tauri 模型测试、根 Rust 测试、fmt、Tauri Clippy、diff-check 通过；未修改用户模型目录或音频数据库。

## 2026-08-28：零启动扫描与轻量输出索引

实施交接：`/private/tmp/W4DJ-zero-startup-scan-lightweight-index-handoff.md`。

- [x] 删除 Tauri setup 中的历史导入路径；启动只初始化并打开 `<app-data>/w4dj.sqlite3`，不遍历旧输出、不读取网易云歌曲表、不执行媒体探测或模型加载。历史导入 API 仅保留为显式兼容入口。
- [x] 新增 `W4djLibrary::upsert_lightweight_output`，在转换最终安全提交后以短事务登记稳定身份、来源/目标路径、槽位和最小标题/歌手信息；不调用 `metadata`、音频 probe、NCM 完整记录或目录扫描。
- [x] 同 ID、同来源或同目标的后续转换复用一条索引记录，迁移目标位置时不访问/删除旧音频；新提交会清除旧分析投影并恢复 `notAnalyzed`。普通转换不再执行转换后的输出目录登记扫描。
- [x] Dashboard 分析候选、DJ 匹配和 M3U8 候选不再按旧可用性状态过滤；导出仅在选中路径上做存在性检查，网易云 SQL 与 `library-dashboard.sqlite3` 不参与歌曲枚举。
- [x] 新增启动零扫描、轻量登记无文件依赖/不触碰输出根状态、ID/来源迁移和失效候选匹配回归测试；兼容直转入口补齐可获得的网易云身份，旧输出清理限定在当前输出根目录；根 Rust 全量及 Tauri 测试、前端 Vitest 198/198、TypeScript、Vite、fmt、Tauri Clippy、放宽既有 dead_code/map_identity 的根 Clippy 和 diff-check 通过。
- [x] 真实跨用户后台启动验收已完成：使用最新 arm64 App 的临时副本和 `open -g`，全新隔离用户 1 秒内启动并创建空 `w4dj.sqlite3`（0 首、0 分析），升级隔离用户 1 秒内启动；升级副本的 `tracks`、`local_files`、`w4dj_track_meta`、`analysis_results`、`w4dj_output_identities` 行内容与生产副本一致，无历史导入或输出遍历。辅助功能权限未授予，窗口计数记录为 `access-denied`，但进程/锁/SQLite 启动证据完整。
- [ ] 真实转换后索引/分析回写、外置 T7/2,398 文件性能、Windows/Rekordbox 和 89 首 FLAC 仍需现场环境；不把未执行项写成通过。

## 2026-08-28：生产包隔离验收资源

- [x] `app/vite.config.ts` 的生产构建关闭 `publicDir`，验收音频、验收页面和工作区模型链接只在工作区使用，不进入 `app/dist` 或正式 App。
- [x] Tauri 继续从 `src-tauri/resources/essentia-models` 只打包一份正式模型；隐藏运行时入口保持兼容。
- [x] 重新构建并核对 arm64 App：约 95 MB，生产 `dist` 约 6.4 MB，包内无 `acceptance-audio`、验收页面或前端模型副本；版本保持 `3.2.0-beta.3`。
- [x] 前端 Vitest 198/198、TypeScript、Vite 构建和体积/路径检查通过；未修改用户数据，未 commit/push/release。

## 2026-08-28：任务 1 网易云“情况”栏

- [x] 任务 1 工具栏增加固定“情况”/“Status”标签，动态值覆盖读取、未就绪、建立中、已就绪、取消、手动数据库、自动回退和错误状态。
- [x] 情况值使用 neutral/running/success/warning/error 语义色，完整文案保留在 `title`，窄屏布局不覆盖网易云操作按钮。
- [x] 缓存进度期间原位更新情况值；仅在按钮可用性或终态改变时重绘，保留既有扫描与数据库操作逻辑。
- [x] 前端 app.test.ts 124/124、TypeScript、Vite 和 diff-check 通过；arm64 App 已重编译，版本保持 `3.2.0-beta.3`。
- [ ] 真实网易云、Windows/Rekordbox 和播放器现场验收仍待对应环境。

## 2026-08-28：任务 1 扫描进度与转换预览口径修复（独立 worktree）

- [x] 扫描任务的阶段计数改为按当前阶段使用独立分母；输入、输出、网易云元数据匹配不再相加，完成态固定显示输入曲目总数。
- [x] 网易云轻量缓存准备进度接入任务 1 进度条；扫描终态在下一次扫描或下一步转换操作前保持显示。
- [x] 转换预览增加输入曲目、输出重复曲目、动态操作数量、数据库目录和逐曲明细 DTO；覆盖模式不会把已有输出计入跳过。
- [x] 确认窗口的预计输出/可用空间改为同一右侧列两行显示，四个统计卡片支持按类别打开 A–Z 歌曲明细。
- [x] 自动化验证：前端 app.test.ts 126/126、TypeScript、Vite、Tauri check、根 Rust 测试和格式检查通过。
- [ ] 真实 1173 首网易云目录的现场扫描、覆盖转换和文件打开链接仍需在用户环境验收。

验收更新（2026-08-28 独立 worktree）：前端全量 Vitest 14 个文件、209/209 通过；TypeScript、Vite 生产构建、根 `cargo test --all`、Tauri `cargo check`、严格 Tauri Clippy、Rust fmt 和 `git diff --check` 通过。已生成 arm64 App；真实 1173 首现场扫描、覆盖转换和文件打开链接仍需用户环境操作。
