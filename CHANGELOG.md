# W4DJ RKB 更新日志

## v3.2.0

### Changed

- 更新项目版本号至 3.2.0。
- 修复空任务阻塞其他任务、转换失败残留半成品等问题。
- 修复 Rekordbox XML 结构、节拍编号和文件路径兼容性。
- 为 WAV 输出补写 Rekordbox 可识别的 RIFF 元数据。
- 改进网易云来源的 MP3/FLAC 文件名与歌手、歌曲名元数据识别。
- Windows 选择来源时支持选择文件夹或单曲。

## v3.1.2

### Improved

- 使用引导扩展为 5 步，并说明如何从右上角“教程”重新查看完整引导。
- 统一高级选项中的中文术语为“歌曲名”和“歌手”。
- WAV/AIFF 无损格式选择器改为从黑色区域平滑浮现，兼容减少动态效果设置。
- 修复扫描后转换、直接转换、普通转换和增强模式切换时的文字闪烁与字重观感不一致。

## v3.0.1-beta.2

### Improved

- 稳定“扫描后转换 / 直接转换”和“普通转换 / 增强转换”的滑动选择器文字渲染。
- 修复快速来回切换时选中标签闪烁、跳动或短暂消失的问题。
- 保留平滑滑动动画，同时固定文字绘制层，减少 WebView 重排和重新栅格化造成的视觉闪烁。
- 延续 3.0 系列的扫描流程、增强转换、教程引导、元数据保护和 macOS DMG 修复工具。

### Breaking Changes

- 3.0 系列引入 Essentia.js 音频分析引擎，发行包新增依赖和许可证义务。
- 开启“增强转换”后，会额外分析 BPM、Key、响度、能量等信息，并写入输出文件元数据；输出文件的元数据结果可能与 2.2.x 不同。
- 安装包体积和运行时资源需求相较 2.2.x 增加。
- “增强转换”默认关闭；关闭后“普通转换”仍保持原有转换行为。

## v3.0.1-beta.1

### Improved

- 使用引导扩展为 5 步，并说明如何从右上角“教程”重新查看完整引导。
- 统一高级选项中的中文术语为“歌曲名”和“歌手”。
- WAV/AIFF 无损格式选择器改为从黑色区域平滑浮现，兼容减少动态效果设置。

## v3.0.2

### Fixed

- 改进 MP3、WAV、AIFF、FLAC 与 NCM 的标题、歌手、专辑与封面写入：无标签或错误标签会根据文件名和来源重新识别，并在写入后复核。
- 过滤无效封面数据，替换过期封面，提升 DJ 软件读取封面的稳定性。
- 每次转换自动生成逐曲元数据诊断报告，记录源/输出标签、封面、文件名判断与校验结果。
- 新增网易云源文件名格式设置：仅歌名、歌手 - 歌名或歌名 - 歌手，真实参与扫描预览与输出命名。
- “关于”页新增手动检查 GitHub 更新；仅在用户点击时联网。
- 修正 macOS Release 产物的 DMG 扩展名，继续保留 Gatekeeper 修复工具、双架构 Mac 包与 Windows 包。

### Kept from 3.0

- 保留后台扫描、扫描后转换/直接转换、增强转换、教程引导和 DMG Gatekeeper 修复工具；未回退到 2.2.5 的旧界面或旧流程。

## v3.0.0

### Added

- 新增可选“加强模式”：开启后使用 Essentia.js 自动分析 BPM、Key、响度、能量、Danceability 和节拍位置。
- 分析结果会写入转换后的音频元数据，并保存在本地分析库中，便于 Rekordbox 读取和后续 XML 导出。
- 加强模式默认关闭；关闭时保持原有的普通转换流程，不读取音频分析数据。
- 单曲分析失败不会中断整批转换，失败路径和原因会写入转换历史及自动错误报告。
- macOS DMG 内新增 Gatekeeper 修复工具；清除隔离属性后会自动打开已安装的 W4DJ RKB.app。

### Improved

- 加强模式开关采用紧凑横向滑动样式，默认窗口仍可完整看到主要操作和转换历史。
- 加强模式状态会保存到本地偏好设置，重新打开软件后继续使用上次选择。

### Fixed

- Release Actions 会校验最终 DMG 同时包含 App、Gatekeeper 修复工具和 Applications 入口。

### Notes

- Rekordbox XML 只引用音频文件路径，不会复制音频文件；导入后请确保原输出目录保持不变。
- Essentia.js 使用 AGPL-3.0 授权，分发包含该模块的安装包时需要遵守对应许可证。

## v2.2.4

### Added

- DMG 内新增「解决文件已损坏问题（双击）」工具。
- 将 W4DJ RKB 拖入“应用程序”后，双击该工具即可清除 macOS 隔离属性。

### Fixed

- 修复未签名 macOS 应用首次打开时可能出现“文件已损坏”提示的问题。

## v2.2.3

### Added

- 测试版本：每次转换自动生成转换报告。
- 支持直接测试 macOS Apple Silicon `.app` 应用。

## v2.2.2

### Fixed

- 修复 MP3、WAV、AIFF、FLAC 和 NCM 转换后文件名正确但标题、艺术家等元数据为空的问题。
- 转换后会补齐缺失的标题和艺术家；已有专辑信息和封面会继续保留。
- 增加无标签音频的元数据回归测试，覆盖文件名识别与封面保留逻辑。

### Breaking Changes

- 无。

## v2.2.1

### Added

- 新增首次打开使用引导，说明输出模式、拖入来源、输出目录和开始转换四步操作。
- 转换历史支持失败项目一键重试、单条删除和清空全部记录；删除历史不会删除已经生成的音频文件。

### Improved

- 优化四个输入/输出拖放区域的命中逻辑，拖入任务 1/2 的任意区域都会只更新对应区域。
- 将转换错误转换为包含原因和处理建议的用户提示，并保留详细错误信息供排查。
- 升级 GitHub Actions 到 Node.js 24 兼容版本，继续构建 Apple Silicon、Intel Mac 和 Windows 安装包。

### Fixed

- 修复拖放文件夹时任务 2 输出目录无法接收、路径错误落入任务 1 输入目录的问题。

### Breaking Changes

- 无。

## v2.2.0

### Added

- 每个任务的输入来源现在同时支持歌曲文件夹和单曲文件，可通过文件选择器或拖放添加 MP3、FLAC、NCM、WAV、AIFF。
- 新增输出文件冲突策略：已存在文件时跳过或覆盖；高级选项支持仅更新元数据。
- 新增文件名规则设置，可选择“标题 - 艺术家”“艺术家 - 标题”或保留原文件名；默认仍为“标题 - 艺术家”。
- 新增任务取消功能，可取消单个任务或全部运行中的任务；当前歌曲会安全结束后再停止。
- 新增未完成任务恢复机制。任务进度会持续保存，暂停、取消或应用意外关闭后，可从转换历史继续剩余文件。
- 新增磁盘空间检查。转换前显示目标磁盘可用空间，空间不足时会阻止任务开始并说明原因。
- 新增错误分类，区分文件损坏或无法读取、格式不支持、FFmpeg 失败、输出目录无权限、磁盘空间不足、文件名非法和其他错误。
- 新增历史记录管理，可删除单条历史或清空全部历史，不会影响已经生成的音频文件。
- 新增“关于”窗口，可查看应用版本、开发者和项目主页。

### Improved

- 重做默认紧凑布局：任务详情、日志和高级选项默认收起，打开软件即可完整看到任务区与转换历史。
- 输入来源自动识别文件夹或单曲，移除“选择单曲”按钮；任务卡片保留清晰的来源、输出目录和转换进度。
- 优化任务卡片和清除按钮的对齐、留白与图标位置，兼容模式与无损模式切换时布局保持稳定。
- 转换历史移动到任务区下方并支持折叠，默认窗口尺寸调整为适合完整显示主要操作。
- 自动清理跨平台不允许的文件名字符，处理 Windows 保留名称、末尾空格和句点，并限制过长文件名。
- “仅更新元数据”会直接更新现有 MP3、WAV 或 AIFF 输出，不重新编码音频；支持从 MP3、WAV、AIFF、FLAC 和 NCM 读取元数据。
- 转换历史会持续记录剩余项目与已完成数量，并支持在重新打开应用后继续任务。
- 两个并行任务写入历史记录时会串行保存，避免记录相互覆盖。
- 预检窗口现在同时显示预计输出大小和目标磁盘可用空间。

### Fixed

- 修复单曲路径会被预检和正式转换误判为“不是目录”的问题。
- 阻止输出目标与输入单曲相同时覆盖或修改原曲，并确保清理旧格式时不会删除位于输出目录中的源文件。
- 修复两个任务计划写入同一输出文件时可能重复转换的问题；重复项目现在只交由前一个任务处理。
- 修复取消或暂停后只能重新开始整批任务、无法准确继续未完成文件的问题。
- 修复覆盖和仅更新元数据策略在预检结果与实际目标文件之间可能不一致的问题。
- 修复“仅更新元数据”可能错误使用推算目标路径，而不是实际已存在文件路径的问题。
- 修复转换历史中的跳过数量可能被错误记录为全部已存在文件数量的问题。
- 修复两个任务同时结束时可能竞争写入历史文件、导致部分历史丢失的问题。

### Breaking Changes

- 无。v2.1.0 的偏好设置与历史记录会自动兼容；新增字段使用安全默认值。

## v2.1.0

### Added

- 新增转换前汇总确认窗口，一次查看新增、已存在、将跳过和错误文件数量。
- 新增预计输出大小，开始转换前即可了解大致磁盘空间需求。
- 新增转换历史记录，保存任务时间、文件数量、失败文件和输出目录。
- 新增失败任务重试，可只重试失败歌曲，并支持导出错误报告。

### Improved

- 确认窗口与实际转换使用同一份预检结果，避免确认后统计与实际执行不一致。
- 历史记录按任务集中展示，方便追踪多次转换结果。
- 输出模式和 WAV/AIFF 选择操作更稳定，快速操作时界面响应更连贯。
- 更新使用说明，补充预检确认、历史记录和失败重试的使用方式。

### Fixed

- 修复反复点击兼容模式时滑动动画不断重新播放的问题。
- 修复快速切换 WAV 和 AIFF 时按钮短暂卡顿或状态不同步的问题。
- 修复部分任务失败后无法明确查看失败文件的问题。

### Breaking Changes

- 无。

## v2.0.1

### Added

- 支持两个同步任务同时配置和执行，适合批量处理网易云音乐与 SoundCloud 曲库。
- 支持兼容模式输出 MP3，以及无损模式输出 WAV 或 AIFF。
- 支持网易云音乐 `.ncm` 解密转换，并支持常见音频文件处理。
- 支持保留歌曲元数据和封面，生成适合 DJ 软件和设备使用的音频文件。
- 提供中英文界面、浅色/深色主题和拖放导入等基础操作体验。
- 提供 Apple Silicon、Intel Mac 和 Windows 安装包。

### Improved

- 优化批量转换进度、任务状态和输出目录配置体验。
- macOS 安装包使用易懂的 Apple Silicon 与 Intel 文件名，便于选择正确版本。

### Fixed

- 修复多来源音频转换和不同输出格式处理中的稳定性问题。
- 修复部分元数据、封面和输出文件命名在批量处理时不一致的问题。

### Breaking Changes

- 无。
